# 安全规则

- 不把 API key、密码、Token、正式密钥或完整隐私数据写入代码、日志、事件和报告；
- 测试默认使用隔离环境、测试租户和沙箱凭据；
- 真实付款、下单、客户消息、正式数据删除和破坏性接口默认禁止；
- 生产构建不得注册危险测试路由、故障注入和破坏性清理能力；
- 外部发布、权限变更、推送、删除和不可逆迁移需要明确授权；
- 测试平台故障不得阻断正式业务，也不得因为观测失败补写成功结果。

