# 变更控制规则

## 变更前

- 关联任务 ID、设计章节和影响模块；
- 读取 Git 状态和当前基线；
- 判断是否影响接口、数据、权限、外部副作用或迁移；
- 高风险变更先取得人工确认。

## 变更中

- 只修改任务范围内的文件；
- 不使用破坏性 Git 操作覆盖用户工作；
- 发现阻塞项记录，不静默绕过；
- 保留可回滚的中间状态。

## 变更后

- 运行语法、单元、集成、浏览器或契约检查；
- 更新任务状态和变更说明；
- 提交信息必须说明目的和验证结果；
- 发布前确认文档、映射和报告同步。

