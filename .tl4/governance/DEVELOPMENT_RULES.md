# 开发规则

- 目录按职责分层，业务代码、测试、文档、运行数据和报告不得混放；
- 新模块先定义边界和接口，再写实现；
- 外部依赖通过适配器、接口或依赖注入接入；
- 运行数据、日志、上传文件、本地凭据放在 `runtime/`，不提交到源码；
- 修改代码必须补充或更新相应测试；
- 大规模重写前先保存可恢复的基线；
- 使用项目已有工具和脚本，不重复创建等价入口；
- 任何成功结论必须有命令、测试或浏览器证据支持。

