# 文档同步规则

- 产品目标写入 `docs/product/` 或产品主文档；
- 架构、接口、数据和模块边界写入 `docs/architecture/`；
- 任务和阶段拆分写入 `docs/plans/`；
- 设计规则、代码位置、业务事件和测试剧本保持双向映射；
- 文档中的“已实现、已验证、建议、阻塞”必须明确区分；
- 网站和控制台不复制项目设计文档原文，只引用权威文档或读取摘要。

