# 需求池 ｜ BadgetManager（三安光电 AI 费用预决算管理系统）

> **定位**：Sponsor（松哥）→ 项目经理（PM）的**原始需求入口**。本文件**只收需求，不做任务拆解**。
> **流程**：松哥扔需求 → PM 组织 / 拆解 → 形成任务 → 入《任务看板 TASK_BOARD.md》→ 分派开发 agent。
> **与任务看板的关系**：需求池与任务看板是**两个独立载体**，不混写。需求池里的条目经 PM 拆解后才转化为 TASK_BOARD 的任务（可一对多）。
> **参照**：HELPBUY 需求池机制。
> **纪律**：未经 PM 拆解的需求不进开发；需求状态由 PM 维护。

---

## 需求列表

| 编号 | 日期 | 标题 | 关联模块 | 状态 |
|---|---|---|---|---|
| REQ-001 | 2026-09-04 | 预算规划：经济事项 ↔ 预算规则卡绑定方式优化（两种可切换模式） | M6 规则引擎 / `rules` 视图 Tab3「适用经济事项」 | 开发完成（v1.5.6） |
| REQ-002 | 2026-09-04 | 预算跟踪页优化：新增「预算排行榜」（超 50%/30%/10% 分档图表，支持组织层级 + 时间层级） | `kanban` 预算跟踪视图（主）+ 可选 `track` 预算追踪视图 | 开发完成（v1.5.6） |
| REQ-003 | 2026-09-04 | 编制页优化：新增「编制理由」列（AI 建议之外，编制者自填编制理由） | `compile` 预算编制视图（经济事项视角 8 列填报表） | 开发完成（v1.5.6） |
| REQ-004 | 2026-09-04 | 编制流程优化：规则驱动的「自动分解」事项免手填（如人均标准/业务量类，系统按规则自动算预算） | `compile` 预算编制视图（方法体系 `CTRL_METHOD_ASSIGN` / `compileByMethod`） | 开发完成（v1.5.6） |

---

## 需求详述

### REQ-001 · 预算规划：经济事项 ↔ 预算规则卡绑定方式优化（两种可切换模式）

- **来源**：松哥（Sponsor），2026-09-04
- **原始意图（松哥原话）**：优化预算规划里面的经济事项与预算规则卡的绑定方式。建议采取两种方式，可切换：
  - **方式一**：左边是规则卡，右边是树形的经济事项。给选定一个规则卡，去找经济事项。
  - **方式二（反过来）**：左边是经济事项（树形），右边是规则卡。即两个逻辑。
- **当前现状（PM 读 `views/rules.js` 核对）**：
  - Tab3「适用经济事项」现为「上方规则卡横向滚动条 + 注释文本框 + 下方经济事项勾选列表（平铺，非树形）」。
  - 仅支持「选规则卡 → 勾选科目」单向，无反向；经济事项为平铺 checkbox 列表，无树形层级。
- **期望目标**：
  - 提供**可切换**的两种绑定视角：
    1. 规则卡（左）→ 树形经济事项（右）：选中规则卡，在树中勾选/定位关联经济事项。
    2. 经济事项（左，树形）→ 规则卡（右）：选中经济事项节点，绑定对应规则卡。
  - 经济事项以**树形**呈现（关联 E1–E6 已完成的经济事项 4 级分类重构，146 树 / 300 事项）。
- **关联**：M6 客户规则引擎、经济事项 4 级分类树（E1–E6）、`BM.RULE_EVENT_MAP`（规则卡字典）、`/api/rule-versions/{id}/event-map`（映射持久化）。
- **状态**：开发完成（v1.5.6 · RK1 双向绑定树形已落地，全量回归绿）。
- **备注**：本需求属于交互/信息架构优化，非新增业务规则；改动限制在 `rules.js` Tab3 + 相关 CSS，须配套回归（现有 `rules_event_map.e2e` 22/0 等）。

### REQ-002 · 预算跟踪页优化：新增「预算排行榜」（超 50%/30%/10% 分档图表，支持组织层级 + 时间层级）

- **来源**：松哥（Sponsor），2026-09-04（松哥称「第二条需求」）
- **原始意图（松哥原话大意）**：优化预算跟踪页面。按客户要求，无论跟踪频率如何，都希望能有一个「预算排行榜」——哪些超了？超 50% 的、超 30% 的、超 10% 的，分档用图表展示，总之是增加可读性。并且这种排行榜要符合不同层级，包括组织层级和时间层级。
- **当前现状（PM 读 `views/kanban.js` + `views/track.js` 核对）**：
  - **`kanban` 预算跟踪视图**（三层时间 年/季/月 + 角色×组织范围 + 经济事项/财务双维度嵌套）：节点卡展示 预算/执行/执行率，超支/节支 badge（`devPct`），进度条，超支预警/临界。但**没有「排行榜」**——卡片按组织树/维度平铺，不按超支幅度排序；无「超 50%/30%/10%」分档聚合图。
  - **`track` 预算追踪视图**（月度执行追踪 1-9 实际 + 10-12 预测）：有 AI 预警摘要（danger/warn 科目列表）、三态筛选（全部 / 当前超预算 / 当前有结余 / 预计有结余）、月度执行明细表、AI 偏差归因。**无排行榜图表**——「当前超预算」筛选只是计数 badge，不是按幅度排序的分档排行榜。
  - **结论**：两个视图都**不存在**「按超支幅度分档（超50%/30%/10%）并排序展示」的排行榜图表。层级支持上：`kanban` 已天然具备「组织层级（组织树下钻）+ 时间层级（年/季/月面包屑）」，与松哥要求的「组织层级 + 时间层级」完全吻合；`track` 有角色范围但不含显式组织/时间树。
- **期望目标**：
  - 在预算跟踪页新增「预算排行榜」模块/图表：按超支幅度**分档**——超 50% / 超 30% / 超 10% / 未超，并按幅度**降序排序**，一眼看出「哪些超了、超多少」。
  - 提升可读性：用图表（排行条/柱状）替代纯列表；超支档位用醒目色（呼应现有 danger=红 / warn=橙 / ok=绿 体系）。
  - 须符合不同层级：① **组织层级**——按当前角色可见的组织范围（事业部/管理中心/部门/单位）聚合排名，随下钻切换；② **时间层级**——按 年/季/月 当前选定期间聚合排名，随面包屑切换。即排行榜口径随 `kanban` 现有的「组织层级 + 时间层级」联动。
  - 与 `kanban` 现有「当期切片（预算/执行/偏差/预警）」卡片并存，作为新增一屏/区块，不替换原卡片。
- **关联**：`kanban.js`（预算跟踪，三层时间 + 组织范围）、`track.js`（预算追踪，月度执行）、`BM.kanbanAgg`（聚合）、`BM.orgTreeCache`/`BM.orgChildren`/`BM.orgCompanyCodes`（组织树）、`BM.scopeDeptIds`/`BM.scopedData`（角色范围）、`devPct`/`rate` 计算（纯函数）。
- **状态**：开发完成（v1.5.6）。
- **备注**：① 属信息架构/可读性优化，非新增业务规则；改动限制在 `kanban.js`（主）+ 相关 CSS，可选扩展 `track.js`；须配套回归（现有 kanban E2E、wb-home-diff 等）。② 需区分两个同名近义视图——本需求**主目标为 `kanban` 预算跟踪**（因其天然带组织 + 时间双层），`track` 预算追踪作为可选扩展面。③ 与 REQ-001 同属「交互/可读性优化」类，可一并规划但分别拆解。

### REQ-003 · 编制页优化：新增「编制理由」列（AI 建议之外，编制者自填编制理由）

- **来源**：松哥（Sponsor），2026-09-04（松哥称「第三条」）
- **原始意图（松哥原话大意）**：在预算编制页面里，除了 AI 给出的建议之外，编制者还可以写自己的「编制理由」——也就是增加一列，让编制者填写自己的编制理由。
- **当前现状（PM 读 `views/compile.js` 核对）**：
  - 编制工作台「经济事项视角」为 **8 列填报表**：经济事项 / 会计科目 / 本年度预算值 / 上年预算 / 上年决算 / 偏差 / **AI 建议** / 月度拆分。**没有「编制理由」列**。
  - AI 建议列（`ai-suggest-th`，`compile.js:159` / `:224`）：展示 AI 区间 + 采纳中值 + 💡 编制建议弹层（含 `basisNote` / `rangeReasoning` 等 AI 依据）。
  - **关键发现：数据层已预留 `reason` 字段**——`BM.compileSaveSubject`（`compile.js:92`）与 `BM.compileSaveDraft`（`compile.js:435`）的 subject item 都带 `reason: ""`，只是当前**从未被赋值/展示**。即接入成本很低：UI 加列 + 行内输入 + 落库 `reason`，无需新增数据模型/迁移。
  - API 模式注意：`/api/events/{id}/amount` 当前 PUT 仅传 `{ amount }`，若要后端持久化理由需后端配合扩展（属后端扩展点）。
- **期望目标**：
  - 在编制工作台「经济事项视角」增加「编制理由」列（建议紧邻 AI 建议列之后、月度拆分之前，形成「AI 依据 ↔ 我的理由」对照）。
  - 该列为可编辑输入（textarea/input），编制者填写自己这样编的理由（人工说明）。
  - 理由随行保存：离线草稿模式落 `compileSaveDraft` 的 `items[cat].reason`（已有字段）；刷新/重开可恢复。
  - API 模式：需后端 `/api/events/{id}/amount` 或新增理由接口支持 `reason`（后端扩展点，拆解时一并考虑，或先离线草稿模式落地）。
  - （可选，后续）汇总/审批页（如 `approval`）可展示编制者理由，本需求先不过界。
- **关联**：`compile.js`（`renderFillTable` 8 列、行内 `persist` / `saveBtn` 落库）、`BM.compileSaveSubject` / `BM.compileSaveDraft` / `BM.compileLoadDraft`、`BM.aiSuggestion`、advice overlay（`basisNote` / `rangeReasoning`）、后端 `/api/events/{id}/amount`（reason 扩展点）。
- **状态**：开发完成（v1.5.6）。
- **备注**：① 数据层已预留 `reason` 字段（`compile.js:92` / `:435`），**改动成本低**，主要是 UI 加列 + 行内输入 + 落库，无需数据模型迁移。② 属信息架构/可读性优化类，与 REQ-001/002 同类。③ 须配套回归（compile 视图 E2E、草稿恢复）。

### REQ-004 · 编制流程优化：规则驱动的「自动分解」事项免手填（如人均标准/业务量类）

- **来源**：松哥（Sponsor），2026-09-04（松哥称「第四条」）
- **原始意图（松哥原话大意）**：预算编制过程中，有些事项是「填报数据」，有些是「直接分解」。规则先把它定义好——比如「人均伙食费」是均摊的，编制时就直接分解成「每人多少」，遇到这种规则，系统自动帮他把预算做了分解，不用再去手动编。
- **当前现状（PM 读 `core/calc.js` + `data/rules-engine.js` + `views/compile.js` 核对）**：
  - **方法体系已完整**：`BM.CTRL_METHOD_ASSIGN[cat]` 给每个经济事项挂了「预算控制方法」（compile.js:4 注释「由上级定义，基层不自选」，与松哥「规则把它定义了」一致）。方法含 历史参考/同比/固定/数量×单价/**人均标准**/业务量/管理标准/关键事件/人工。
  - **自动分解的「引擎」已存在**：`BM.calc.compileByMethod`（calc.js:130/134/141）已支持 **`perCapita`（人均标准 = 在编人数 × 人均，desc 正是「如食堂/宿舍」）**、`volume`（业务量×单位成本）、`keyEvent` 等**确定性计算**，返回值 `{ amount, note }`，`note` 已含「人均标准：h 人 × pc」的分解口径。
  - **缺口在 UI 层**：`renderFillTable`（compile.js:152）对**每一行都渲染「本年度预算值」的滑块+数值输入（手动填）**，不分方法；`baseline` 虽由 `compileByMethod` 算出，但用户仍被要求手填滑块。即**没有「规则定义→自动分解、免手填」的行状态/视觉区分**——这正是本需求要补的。
- **期望目标**：
  - 编制页按规则（`CTRL_METHOD_ASSIGN` 的 method）区分两类事项：**「填报类」**（人工录入，如 manual/需主观判断的）与 **「规则自动分解类」**（perCapita/volume/keyEvent 等确定性可算的）。
  - 对「规则自动分解类」事项：系统按 `compileByMethod` **自动算出预算并填入**，行内明确标注「自动分解」（如展示「人均 X 元 × N 人 = 总额」），**禁用/锁定手动滑块**，实现「不用再去编」。
  - 对「填报类」事项：保留现有手动滑块+AI 建议入口不变。
  - （可选）「自动分解」行的分解口径（如每人多少）可在行内或弹层展示，呼应松哥举例的「分解每人多少」。
- **关联**：`compile.js`（`renderFillTable` 行渲染、`methodContext`）、`core/calc.js`（`compileByMethod` 已支持 perCapita/volume/keyEvent）、`data/rules-engine.js`（`CTRL_METHOD_ASSIGN` 事项→方法映射、`BUDGET_CONTROL_METHODS` 元数据）、`compileSaveSubject`/`compileSaveDraft`（落库）。
- **状态**：开发完成（v1.5.6）。
- **备注**：① **引擎已就绪**——`compileByMethod` 纯函数已支持人均标准等确定性计算，`note` 已含分解口径 → **接入成本低**，主要是 UI 区分「自动分解行 vs 手填行」+ 行内自动填充/锁定 + 展示分解口径，无需改计算内核。② 分类由规则驱动（`CTRL_METHOD_ASSIGN`），与松哥「规则把它定义了」一致。③ 属编制流程/交互优化，与 REQ-001/002/003 同类。④ 须配套回归（compile E2E、test_calc.js 的 `compileByMethod` 单测已覆盖）。
