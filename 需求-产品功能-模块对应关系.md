# 需求-产品功能-模块对应关系（全量版）

> 维护者：**PM（项目经理）**
> 更新时机：每轮需求对齐 / 决策变更后即时更新；每日汇报时同步其变化（并入 `governance/TEAM.md` §2.4、§2.9、§3.3）
> 性质：需求 → 产品功能 → 模块 的三级追踪**全量**单一真源；与《产品功能与代码映射关系.md》（开发工程师维护）互补
> 需求基线来源：
> - `docs/product/三安光电AI费用预决算管理系统需求调研稿V1.md`（初稿，被 V2 覆盖）
> - `docs/product/...需求调研稿V2.md`（2026-08-21 会议纪要确认版 · **全量需求主体**）
> - `docs/architecture/三安光电费用预决算-产品设计稿V1.md`（M1–M11 产品模块 + D1–D6 决策）
> - `docs/product/...需求调研稿V3.md`（2026-09-01 决策确认版 · 增量 D1–D5 / P1–P6）
> 初始基线：2026-09-02（本次由 V3 增量版升级为全量版）

---

## 维护规则

1. **需求编号三套并存，各司其职**：
   - **目标 T1/T2/T3** = 一期三目标（V2 §1.2，最高纲领）；
   - **功能 F1–F11** = V2 §5 功能需求（全量主体）；
   - **决策 D/P** = V3 增量决策（D1–D5 已拍板 / P1–P6 待议）。
2. 每一条需求都追踪到「产品功能（设计稿 M1–M11）→ 前端视图 / 后端模块」两级，并标注**落地现状**（✅ 已实现 / ⚠️ 部分实现 / ❌ 未实现）。
3. 需求状态变化（待议 → 已拍板 → 上线）在「现状」列即时更新，并在当日汇报报告变化。
4. 与《产品功能与代码映射关系.md》分工：本文件管「需求→功能→模块」自上而下追踪；开发文件管「功能→代码」自下而上落点；两者经「产品功能」中间层对齐。

---

## 一、需求全景（三层来源）

```
┌─ 纲领层：一期三目标（V2 §1.2）
│    T1 预算编制及统计自动化（目标一 · 基础）
│    T2 简单预算执行跟踪（目标二）
│    T3 AI 费用风险识别（目标三）
│
├─ 功能层：V2 §5 十一项功能需求 → 设计稿 M1–M11 产品模块
│    F1 预算编制  F2 碰撞协商  F3 AI 诊断  F4 水分识别  F5 智能压降
│    F6 预算弹性  F7 关键事件  F8 数理平衡/版本  F9 决算闭环
│    F10 客户规则引擎  F11 AI 费用风险识别
│
└─ 增量层：V3 决策（2026-09-01）
     D1 基础数据提供  D2 费控 API 确认  D3 专网部署  D4 权限移交  D5 填报备注栏（已拍板）
     P1 按小类填报(✅已拍板)  P2 AI 管控建议  P3 跟踪页排序  P4 明细导出  P5 月度报告  P6 滚动预算=月度结转(✅已拍板)
```

---

## 二、核心映射总表：产品功能模块（M1–M11）→ 需求来源 → 代码落点 → 现状

> 产品功能维度以设计稿 V1 的 M1–M11 为权威划分。每个模块标注：承接的需求、代码落点、当前落地现状。

| 产品功能模块 | 承接需求 | 后端模块（`server/modules/`） | 前端视图（`website/views/`） | 现状 |
|---|---|---|---|---|
| **M1 组织与权限**（P0） | F2/F8 权限底座、D4 权限移交 | `auth.js`、`organization.js` | `login.js`、`roleSwitch.js`、`accounts.js`、`basedata.js`、`orgchart.js` | ✅ 已实现（登录/角色切换/组织树/账户/环检测+四道删除守卫）；⚠️ demo 4 角色占位 vs 真实 14 角色模型待重构；D4 超管移交流程待设计 |
| **M2 主数据**（P0） | F1 科目映射、D1 基础数据、P1 小类填报 | `subjects.js`、`events.js`、`organization.js` | `basedata.js`、`orgchart.js` | ✅ 已实现（科目/事项/组织字典 + Excel 导入 + 溯源 + 可编辑）；⚠️ 经济事项~390 项为演示数据待 D1 替换；P1 小类下钻口径待拉齐 |
| **M3 预算编制**（P0） | T1、F1、D5 备注栏、P1 | `budget-compile.js` | `compile.js`、`monthly-split.js` | ✅ 已实现（8 列编制 + 经济事项/财务会计双视角 + 月度分解 + AI 建议区间）；⚠️ D5 备注栏 `reason` 字段已预留、UI 待补；双派生视角切换待补 |
| **M4 版本与平衡引擎**（P0） | T1、F8、F5 重算 | `budget-compile.js` | `kanban.js`、`dashboard.js`、`adjust.js`、`balance.js`、`track.js` | ✅ 已实现（版本化 + 五维汇总一致 + 实时重算 + 看板三视图）；⚠️ 事业部维度报表切换待补 |
| **M5 碰撞与协商闭环**（P0） | T1、F2、F5、P2、P3 | `budget-compile.js` | `collision.js`、`collision-tune.js`、`tune.js` | ⚠️ 部分实现（子公司侧调参 + 碰撞差异展示已实现）；❌ 总部侧「压降下达」+ 双向「协商谈判区」待建；P2 AI 管控建议、P3 排序待议 |
| **M6 客户规则引擎**（P0） | T1、F6、F10、D1 | `rules.js`、`ai-policy-extract.js` | `rules.js` | ✅ 已实现（规则三 Tab + 版本化 + 明年新规则 + 分组归类 + 政策文件 AI 抽取）；⚠️ 压降管理属性（刚性/新老业务）待补全 |
| **M7 AI 诊断与风险筛查**（P2） | T3、F3、F4、F11、P2 | `ai-budget-decision.js`、`ai-gateway.js` | `risk-view.js` | ⚠️ 部分实现（AI 风险筛查「提示非判定」+ 偏差归因已实现）；⚠️ AI 诊断合理区间/建议金额部分 demo 硬编码待接真实 LLM；P2 管控建议待补 |
| **M8 执行跟踪**（P1） | T2、D2、P3 | `budget-execution.js`、`expense-import.js` | `track.js`、`import-view.js`、`unit-summary.js`、`unit-inbox.js` | ✅ 已实现（费控 Excel 导入 + 看板 A+B 真实数据 40824 行 + 执行率/超标预警）；⚠️ 执行数据录入 UI（O2）待补；D2 API 自动同步待对方确认 |
| **M9 决算与基线修正**（P1/P2） | F7、F9、P5 | `budget-execution.js` | `final.js`、`final-risk.js` | ⚠️ 部分实现（决算确认 + 风险复核 + 对标已实现）；❌ 「差异解释 → 基线修正 → 下年预算」完整闭环待补；P5 月度报告推送待议 |
| **M10 审计与证据链**（P0） | F2/F8 留痕、D4 移交 | `middleware/`、`db.js`（审计表） | `decisions.js` | ⚠️ 部分实现（policy_document 审计表 + 决策留痕已实现）；❌ 统一 auditTrail 视图待建 |
| **M11 Copilot 交互辅助**（P0/P2） | F3/F11 辅助、F2 协商辅助 | `copilot.js`、`copilot-retrieval.js`、`ai-gateway.js` | `copilot.js`、`engine.js` | ⚠️ 部分实现（受控 SQL + 白名单 + 回灌作答已实现）；❌ 「双层驱动 + 三级约束」架构待落地 |

> 代码落点详见《产品功能与代码映射关系.md》（开发工程师维护）；后端另有组合根 `db.js`、确定性纯函数 `pure-calc.js`/`pure-rule.js`、鉴权/错误中间件 `middleware/`。

---

## 三、正向索引：功能需求（F1–F11）→ 产品功能模块

| 功能需求（V2 §5） | 归入产品功能模块 |
|---|---|
| F1 预算编制（§5.1） | M3 + M2（映射）+ M6（预填） |
| F2 预算碰撞与协商（§5.2） | M5 + M10（留痕） |
| F3 AI 预算诊断（§5.3） | M7 + M6（规则依据） |
| F4 预算水分识别（§5.4） | M7（初筛）+ M5（压降池） |
| F5 智能压降（§5.5） | M5 + M6（约束）+ M4（重算） |
| F6 预算弹性（§5.6） | M6（弹性属性） |
| F7 关键事件管理（§5.7） | M9（关键事件） |
| F8 数理平衡与版本管理（§5.8） | M4 + M10（变更留痕） |
| F9 决算与预算闭环（§5.9） | M9 |
| F10 客户规则引擎（§5.10） | M6 |
| F11 AI 费用风险识别（§5.11） | M7 |

---

## 四、一期三目标 → 模块 → 现状

| 目标 | 内容 | 产品功能模块 | 现状 |
|---|---|---|---|
| **T1 预算编制及统计自动化**（目标一 · 基础） | 统一窗口/科目/填报、自动汇总、多维度报表、调整确认、查询提效 | M2/M3/M4/M5/M6 | ✅ 主体已实现（M1–M18 交付）；⚠️ 事业部维度报表、总部侧压降下达待补 |
| **T2 简单预算执行跟踪**（目标二） | 费控导出 → Excel 导入；预算 vs 实际、执行率、超标预警；事后分析为主 | M8 | ✅ 已实现（导入 + 看板真实数据）；⚠️ 录入 UI、API 同步待补 |
| **T3 AI 费用风险识别**（目标三） | 错科目/费用转移/异常增长/报销异常/结构异常/高风险单位；AI 仅提示 | M7 | ⚠️ 部分实现（提示层已实现）；❌ 真实 LLM 接入 + 管控建议待补 |

---

## 五、V3 增量决策 → 模块 → 现状

**已拍板（D1–D5，可直接进开发）**

| 需求 | 内容 | 产品功能模块 | 代码落点 | 现状 |
|---|---|---|---|---|
| D1 | 基础数据提供（经济事项/组织/历史预算） | M2 | `basedata.js`/`orgchart.js` + `subjects.js`/`events.js`/`organization.js` | ✅ 已拍板 · ⏳ 待对方供数替换演示数据 |
| D2 | 费控 API 确认（有 API 同步 / 无 API 手动） | M8 | `import-view.js` + `expense-import.js` | ✅ 已拍板 · 一期手动已有，API 待对方确认 |
| D3 | 专网部署 + 与费控同环境 | 部署形态 | — | ✅ 已拍板 · 实施阶段项（注：设计稿 D5 原为公有云，V3 更新为专网） |
| D4 | 交付后最高权限移交、开发方无访问 | M1 | `accounts.js`/`login.js` + `auth.js` | ✅ 已拍板 · 超管移交流程待设计 |
| D5 | 填报页备注说明栏 | M3 | `compile.js` + `budget-compile.js` | ✅ 已拍板 · `reason` 字段已预留、UI 待补 |

**待议（P1–P6，须先对齐）**

| 需求 | 内容 | 产品功能模块 | 代码落点 | 现状 |
|---|---|---|---|---|
| P1 | 按经济事项「小类」填报 | M3/M2 | `compile.js` + `subjects.js`/`events.js` | ✅ 已拍板 · 按最细经济事项填报，小类作聚合展示层 |
| P2 | 强化 AI 管控建议输出 | M7 | `track.js` + `ai-budget-decision.js` | ⚠️ 待议 · 现有仅预警/归因 |
| P3 | 跟踪页按超标/执行率排序 | M8 | `track.js` + `budget-execution.js` | ⚠️ 待议 · 排序逻辑缺失 |
| P4 | 预算明细表导出 | M3/M4 | `compile.js` + `budget-compile.js` | ⚠️ 待议 · 口头承诺、未落地 |
| P5 | 月度总结报告推送 | M9 | （消息中心）+ `notifications.js` | ⚠️ 待议 · 通知已有、报告未落地 |
| P6 | 滚动预算（月度结转） | M8 | `pure-calc.js` + `track.js`（可用 = 本期 + 上期结转） | ✅ 已拍板 · 按月结转 / 全部科目可转 / 不设上限 |

---

## 六、一期明确不做清单（边界纪律，防范围蔓延）

> 来源：V2 §1.3 / §7.2（会议纪要正式确认）。以下暂不作为一期核心范围。

1. 全面改造现有费控系统
2. 替代现有报销系统
3. 强制事前锁预算
4. 复杂实时费控
5. AI 自动审批
6. AI 直接判定违规
7. AI 直接决定压降金额
8. 一次性建设完整集团财务管理平台

---

## 七、影响设计的待确认项（V2 §8 关键项 + 设计稿 §8.2）

> 完整清单见 V2 §8（21 项）与设计稿 §8.2（14 项）。以下为影响权限/映射/密级的高优先级项：

| # | 待确认项 | 影响模块 |
|---|---|---|
| 1 | 11 职能中心完整清单 + 科目归口矩阵 | M1/M2 |
| 2 | 财务口径(~220) vs 管理口径(~390) 科目映射与差异归因 | M2 |
| 3 | 事业部 ↔ 法人公司归属清单 | M1/M4 |
| 4 | 降本目标基线年份与降幅确定方式 | M6 |
| 5 | 费用归属 vs 预算归属错位处理规则 | M1/M4/M8 |
| 6 | 敏感数据权限控制规则（密级与可见范围） | M1 |
| 7 | 横向对标「成熟/新模块」分组清单与费用率基线 | M5/M7 |

---

## 八、落地现状总览

| 维度 | 数量 | 状态 |
|---|---|---|
| 产品功能模块（M1–M11） | 11 | 6 全实现（M1/M2/M3/M4/M6/M8）+ 5 部分实现（M5/M7/M9/M10/M11） |
| 一期三目标 | 3 | T1/T2 主体完成，T3 部分 |
| 已拍板决策（D1–D5） | 5 | 均待数据/实施，D5 改动最小 |
| 待议决策（P2–P5） | 4 | 轻量功能优化，可一次对齐 |
| 明确不做 | 8 | 边界锁定 |

---

## 九、增量需求批次 · REQ-001~004（2026-09-04 入池 → RK1~RK4 交付 v1.5.6）

> 来源：松哥 2026-09-04 批量入池（`需求池.md`），PM 拆解为 TASK_BOARD RK1~RK4，弹框拍板后整批开发。

| 需求 | 内容 | 产品功能模块 | 代码落点 | 现状 |
|---|---|---|---|---|
| REQ-001 | 规则卡 ↔ 经济事项两种可切换绑定（+ 树形） | M6 | `views/rules.js`（Tab3 双栏 + 模式切换 + 树形）+ `styles/components.css` | ✅ 已实现（v1.5.6） |
| REQ-002 | 预算跟踪页「预算排行榜」（超50/30/10 分档柱状图） | M4（kanban） | `views/kanban.js`（`renderRank`）+ `styles/components.css` | ✅ 已实现（v1.5.6） |
| REQ-003 | 编制页「编制理由」列（= D5 备注栏） | M3 | `views/compile.js`（落 `reason` 字段）+ `styles/views.css` | ✅ 已实现（v1.5.6） |
| REQ-004 | 编制流程·规则驱动「自动分解」免手填 | M3 | `views/compile.js`（perCapita/volume 锁定）+ `styles/views.css` | ✅ 已实现（v1.5.6） |
