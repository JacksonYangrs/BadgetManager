# 2026-08-22 工作日志

## 三安项目 · 客户经济活动定义（经济活动事项）交付状态核查（PM 视角）
- 客户已给：文件1（总经办14科目逻辑·管理口径）、文件3（财务部会计科目↔经济事项映射·财务口径），会上展示 ~390 项经济事项表。
- 待正式交付：完整 ~390 项经济事项基础表（V2 §9.1 资料1「已展示，待正式交付」）。
- 待拍板：财务(~220) vs 管理(~390) 双轨映射归因规则（V2 §8-16）。
- 待提供：费控数据导出样表（资料2）、当前预算模板/组织架构/事业部关系/历史样例/审批规则（资料3）。

## PM 结论：手上经济活动事项足够页面设计，不够真实数据接入
- 页面设计只需「字段结构 + 代表性样例 + 双轨概念」，三者已具备 → UI Agent 可全速推进，用 sample + 占位 + TODO 兜待交付项。
- 全量 ~390 主数据 + 映射规则属 M2 实现阶段阻塞，非设计阶段阻塞；不要让 Agent 把缺失当设计阻塞。
- 当前 demo 用 6 个 mock 科目作「同源样例」（BM.DUAL_TRACK，compile.js TODO V2 §8-16），策略正确。

## 编制填报 UX 讨论（松哥关注视觉疲劳 + AI 加速）
- 疲劳根因：全量平铺 + 逐项手动。解法=例外驱动（规则预填默认采用，只审偏离）+ 分组折叠树 + 分类批量套用 + 保留 Excel 手感（行内编辑/Enter/Tab/粘贴一列）+ 月度分解内联 + 进度计数 + 双轨一次填 + 新事项标星。
- AI 加速填报：自然语言指令 → AI 抽科目/动作/金额 → 映射为对 core/calc.js 调用 → 确定性算金额 → 写回+高亮+留痕。边界：AI 不算钱、只翻译与执行；改动必留痕；偏离原因仍人填。当前阶段可纯前端规则匹配验证交互，真 LLM 接入只换抽取层（AWS Key 已配）。

## 角色模型 + 切换器（Stage 2，已开发并 PM 亲自验证）
- 现状：BM.ROLES 已含 12 角色（旧4+boss/manager/staff/finance + 真实8：ceo/cooLead/cooAnalyst/legalHead/adminHead/companyBudgeter/centerOwner/expense）；11 职能中心、7 类基层费用数据齐；登录页遍历 BM.ROLES 渲染全部角色卡。
- 缺口已补（UI Agent）：① 登录页给 centerOwner 加职能中心下拉、expense 加费用类型下拉、法人公司层加公司下拉；② 顶部「切换角色」按钮从退出重登改为 in-app 角色切换面板（12 角色全覆盖 + 条件参数下拉，轻量切换不 reload）。
- PM 亲自复跑（cd website）：node --check 改动文件全过；test_calc.js 16/16；smoke_dom.js 12 项全绿。环境无 Chromium，未做端到端浏览器验证（如实标注）。
- 本地预览：已起 8123 静态服务（python http.server --directory website），HTTP 200 可用。

## 设计重大修正：双轨（双数据源）→ 单数据源 + 双派生视角（松哥 22:53 纠正）
- 松哥纠正：系统不应"财务一套数据、管理线再收一套"——基层只填一次（唯一事实源），管理线下指标/压降是对同数据的调整，派生结果并入财务视角，绝不留同一经济事项两份数据。
- 核查：V2 主体已是"统一入口、基层只填一次、财务口径不再二次收集"（§3.4.4/§5/§3.3），但 §3.4.1 图 + §3.4.3 残留"管理口径二次收数"双数据源措辞，与设计目标内部矛盾。
- 已改文档（措辞统一为"单数据源·双派生视角"）：V2 §3.4.1 图、§3.4.3 第249行（保留现状描述+标注待消除）；设计稿V1 §1.3 硬约束1/7、§5.1 图+标题、§5.7；映射文档 §1.2/§3/§4.1/§4.5/待确认项/结论约10处。
- 已改前端（UI语义澄清）：compile.js 双轨段→"单数据源·双派生视角"标题+hint；data.js ROLE_HINTS.compile.expense、expense.desc、DUAL_TRACK 注释。
- PM 亲自验证（cd website）：node --check compile.js/data.js OK；test_calc 16/16；smoke_dom 通过。
- 附：经核查"九种编制方法"非客户原话（客户零散提同比/固定/数量×单价/人均标准/业务量驱动/关键事件约6种），是设计稿V1§209+前端归纳，历史参考/管理标准/人工为补——属待客户确认假设，演示勿当客户已确认口径。

## 明早汇报（09:00 自动化）+ 松哥 3 点指示补答（PM 提案，待拍板）
- 产出 docs/dev-report-2026-08-22-morning.md。本汇报仅分析与提案，未动设计稿/UI。
- ① 视角索引：经济事项视角以经济事项为索引、财务视角以会计科目(accountCode)为首要；建议 compile.js 重构为 event/account/center 三视角+并排比对（同数据不同主轴，与单数据源双派生视角自洽）。
- ② 主工作台重构：编制是年度短窗口(≤1月,常≈1周)非日常主轴，日常=报销监控/预算执行跟踪/异常预警/待我审批；提议主工作台=运营监控中枢+编制独立模块，待办瘦身（日常只显"今天可动作"项）。影响映射文档§4.1、BM.roleViews/defaultView、wb-home、compile.js 入口权重。
- ③ 九种编制方法来源已明确（见上条附记），演示须标注"待确认假设"，安排向客户核实或收窄为客户认可6种。
- 门禁：松哥就 ①②③ 拍板后，再派 UI Agent 改 design doc(V1/V2/映射)+compile.js+wb-home，PM 复跑校验后交付预览。

## 概念纠正（松哥 23:28）："编制方法"→"预算控制的方法"，且由上面定义
- 松哥纠正两点：① 不叫"编制方法"，叫**「预算控制的方法」**；② 方法是**上面（管理层/集团/中心负责人）统一定义**的，不是每个人照着方法来编制。
- 含义：方法是挂在事项上的**自上而下控制手段**（约束/核对基层申报），不是基层自选的编制套路。与"单数据源+管理线下指标/压降"一致。
- 已核实当前设计冲突：`compile.js` 第180-188行让填报人在每行「编制方法下拉」(`<select class="cmp-method">`)自选方法+"按法计算"生成基线——正是被否定的"每个人照着方法来编制"模型，须改为：方法上级预挂(只读)+系统按该法生成"控制基线"+比对偏离。
- 报告 docs/dev-report-2026-08-22-morning.md 的 ③ 与门禁3 已同步更正为"预算控制的方法/上面定义/待上面定义确认"。
- 待松哥明确"去改"后，再改 design doc(V1/V2/映射)+compile.js(方法列改只读/生成控制基线)。数量9 vs 6 仍按"待上面定义确认"占位。

## 实施：三点全改（松哥 23:35 "其他两个就按我们新的计划来改。去改"）
- ③ 预算控制的方法：data.js `BM.COMPILE_METHODS`→`BM.BUDGET_CONTROL_METHODS`+注释"自上而下定义"；新增 `BM.CTRL_METHOD_ASSIGN`(8 个 RULES 科目→上级下发方法)；compile.js 每行「编制方法下拉」→只读显示(上级定义)+「生成控制基线」按钮(按上级方法派生基线比对偏离)；calc.js 注释同步；ROLE_HINTS.compile 去除"九法任选"等基层自选措辞；views.css 加 .ro-tag/.ctrl-method-ro。
- ① 视角索引：compile.js dualModes fin/mgt/both→event(经济事项索引)/account(会计科目首要索引·聚合)/center(职能中心索引·聚合)/both(并排对照)；新增 aggBy 聚合；section 标题改为"经济事项/会计科目/职能中心"。
- ② 主工作台重构：app.js `BM.defaultView` 全角色→"wb-home"(运营监控中枢)；workbench.js 原"今日待办"→"运营监控中枢"(待审/风险预警/执行监控，staff 含报销在途+项目预算)+ 新增"编制季(年度窗口)"入口卡片(非编制期只读/倒计时)；删除 buildTodos 旧季节性待办。
- 文档同步：设计稿V1 §209 "9种编制方式"→"预算控制的方法(上面定义,基层不自选)"；映射文档 §角色视图 基层/compile 默认首屏改为 wb-home、compile 降为模块。
- 验证(PM 复跑)：node --check data/compile/calc/app/workbench 全 OK；test_calc 16/16；smoke_dom 12 项全绿。环境无 Chromium，未做真实浏览器 e2e。
