# 2026-09-02 工作日志 · BadgetManager

## 14:4x V3 置顶「核心约束与建设目标」纲领，落到产品功能
- 松哥贴两条纲领性引语，并拍板：①同意在 V3 开头新增纲领小节；②关键导向「如何落到产品功能上」。
- 两条纲领：
  1. 「上市公司财务数据在披露前很机密」→ 锚定**部署方向**（承接 D3 专网 / D4 权限移交）。
  2. 邬亮「做系统是为了让更多人进来参与、减轻做预算人的压力」→ 锚定**功能设计导向**（参与式 + 减负）。
- 落地方式：V3 新增 **§0 核心约束与建设目标**，两条各拆成「纲领 → 产品功能落点」表格，标注状态（✅ 已有 / ⚠️ 待议 / 待补），作为全文顶层判据——任何功能增改回到「更安全保密 / 让更多人参与并减负」检验。
- 落点映射：约束→专网部署(D3)/同环境(D3)/开发方无权限(D4)/角色工作台(M1)/密级控制(待确认)；减负→统一入口(M2)/按小类填报(P1)/多口径派生/自动汇总(M3)/按人头分配(闭环待补)/规则预填(M2)/AI建议预警(M2·M4·track)/明细导出(P4)/月度报告(P5)。
- §9 版本说明同步补记 §0 增量。

## 15:0x 固化「开发/测试强制流程」到 TEAM.md V2
- 松哥立开发前三条铁规矩：①角色 Sponsor(松哥)/PM(我)/开发工程师/测试工程师；②开发工程师强制流程；③测试工程师强制流程。
- 产出：`governance/TEAM.md` 升级 **V2**，新增 §2.6「开发工程师强制流程」+ §2.7「测试工程师强制流程」，§1 加角色统称说明，§3.2 开发/测试行引用新流程。
- **开发工程师强制流程（6 步，缺一不可）**：①影响评估（全树去重 + 影响面，依据 `pre-change-impact-analysis` skill）②文件位置决策（是否新建模块，依据 `module-structure-discipline` skill，先写模块规格再动码）③开发+自测 ④报告代码位置 ⑤PM 评审代码 vs 需求/设计一致性 ⑥移交测试。
- **测试工程师强制流程（3 步）**：写用例 → 写方案 → 执行（端到端+真机，blocker 有质量否决权）。
- 角色落地：沿用既有专家包 sanan-frontend-dev / sanan-backend-dev（统称开发工程师）+ sanan-qa-tester（测试工程师）。

## 15:1x 招募两位工程师并行了解现状，汇总团队计划
- 松哥令：「安排其他 agent 了解项目现状，随后给我计划」。
- 用两个 general-purpose 子代理并行摸现状：dev-engineer（开发视角）+ qa-engineer（测试视角），均只读不改码。
- **共识**：M1–M18 全交付，测试 124 单元 + 9 集成 + API + 11 E2E 覆盖主链；待改进=前端全局污染隐患、演示数据需替换、compile.js `reason` 已预留 UI 未接。
- **D 线开发顺序**：D5 填报备注栏（无依赖，立即）→ D1 基础数据替换（等对方数据）→ D4 权限移交（并行）→ D3 专网/D2 费控API（实施阶段）。
- **P 线**：先对齐需求，P6 滚动预算（5 待确认点）最后立项。
- **测试基线**：D1–D5 开发前先补用例；D3/D4 必须真机验证。

## 16:4x 固化「纪律违规汇报机制」到 TEAM.md V2
- 松哥立新规：「如果出现项目纪律违规，PM 第一时间汇报，有奖励」。
- 落地：`governance/TEAM.md` 新增 **§3.6 纪律违规汇报机制**（违规 → PM 第一时间上报 Sponsor，奖励由 Sponsor 判定），§2.4 PM 职责补「纪律监督」一条，V2 变更说明同步补记。
- **松哥拍板（同日 16:5x）**：① 违规判定范围 = 暂列 7 条定稿（未走影响评估/没写模块规格/未及时 git 提交/测试未写用例/未拍板就开发/擅自扩范围/未验证宣称完成）；② 汇报形式 = **双轨并行**（当场对话文字 + 落文件留痕到 TASK_BOARD.md 与当日日志）；③ D5 填报备注栏 **先不动**（等 D1 基础数据到位再排）。

## 15:3x 松哥令「先给现有代码分析评价」，PM 逐文件实证
- 结论（实证 + 证据）：**后端架构健康、前端仍是 Demo 内核、最大风险=业务状态前后端双轨未打通**。
- 后端亮点：db.js 组合根 + 11 限界上下文模块（organization/auth/subjects/events/budget-compile/budget-execution/rules/ai-*/notifications/expense-import）；pure-calc/pure-rule 纯函数确定性计算（无副作用可单测）；middleware/auth.js 权限中间件集中化 + resolveAllowedOrgIds 后端 verifier；跨端口径契约测试 backend-consistency.test.js 锁死前后端 decomposeMonthly 一致。
- 前端问题：window.BM 全局命名空间 + 40+ 视图无模块系统；state.js 存大量 mock 硬编码（SUG001-003、张伟/李静/张明远、厦门三安 2010、requestPurchase 硬编码 IT 12 万）与「TODO 后端接入」标注；engine.js 整段硬编码预测/分析/建议 demo 文案。
- 后端债务：server.js 467 行内联路由（events/subjects/orgs/users/copilot 全内联）与 expense-import/policy_rules 的 buildXxxModule().attach() 两种风格混用；server.js 内联 SQL `db.prepare(...).get(org)` 穿透模块边界；db.js 靠 `try{ALTER TABLE}catch{}` 吞异常做迁移（无版本化 migration）；server.js:23 MARKITDOWN 硬编码本机路径；server.js:233 死代码三元 `? 400 : 400`；README 仍写 `python3 -m http.server`（与 Express 现状脱节）。
- 该评价结论未来开发（D1 替换数据、打通双轨）时复用。

## 15:3x 松哥令「前后端两个 agent 各做一遍代码评审，汇总三方」
- 并行派 fe-dev（前端）+ be-dev（后端）两个只读评审子代理，各读自己域代码，返回结构化评价；PM 汇总三方。
- **三方定性一致**：前后端成熟度均约 6.5/10，定性「Demo 可用、工程化半途」。
- **共识问题（高置信）**：① 双轨数据权威不清（前端 localStorage vs 后端 SQLite）② 前端 mock 硬编码 + TODO 未闭环 ③ server.js 内联路由 vs attach 风格不一致 ④ 迁移无版本化（try/catch ALTER）⑤ 因子口径未完全收口（policy_rules.js 第三份）⑥ README 漂移。
- **fe-dev 独有发现**：esc() 只转义 &<> 不转义双引号（role-hint.js:17）；fetch 不查 res.ok（state.js:362 apiGet、basedata.js:108、unit-summary.js:62、rules.js:49/54）；el/esc/money/fmtMoney 在 30 个 view 重复定义；mobile 与桌面端双份拷贝、无共享内核（mobile 连 calc.js 都没有）；VIEWS 依赖 index.html 顺序加载强耦合。
- **be-dev 独有发现**：无统一 async 错误包装 + 全局错误中间件（单进程崩溃风险）；ai-budget-decision.js:17-21 预留「LLM 直出 lo/hi/mid」破坏「AI 不输出最终金额」红线；server.js:360 /api/orgs 返回 HQ root 未按 scope 裁剪（低危泄漏）；db.js 只聚合 9 模块、copilot/ai-gateway 等 4 个被 server.js 直接 require；README:73 标注 /api/roles 无鉴权（实际需 auth）。
- **be-dev 补充亮点**：copilot-retrieval.js 白名单+全参数化+姓名脱敏；ai-gateway.js AES-256-GCM 密钥加密 + maskSecret + redactSecrets；organization.js 环检测 + 四道删除守卫。
- **PM 综合优先级**：P0 低成本（统一 async 错误中间件 / 前端统一 fetch 层 / esc 补引号+收敛工具函数）→ P1 架构收口（三份因子合一、数据权威收敛+闭环 TODO、版本化迁移）→ P2 重构（server.js 内联路由收编、mobile 共享内核）。

## 17:5x P0-2 收口最后一环：抽共享模块 core/api.js，修复移动端登录
- 背景：松哥拍板「本轮先收口完 P0-2」；发现统一请求层只搭了骨架、移动端（加载自己的 mobile/core/state.js 旧版，无 apiLogin/apiGet/apiSend）登录即崩（`BM.apiLogin is not a function`）。松哥定「仍要修好移动端再闭环」+ 选「抽共享模块 core/api.js」。
- 产出：新建 `website/core/api.js`（apiLogin/apiFetch/apiGet/apiSend/handleSessionExpired/logout；内部 state.→BM.state.、saveState→BM.saveState；renderLogin/toast 晚绑定）；桌面 `core/state.js` 删同名 6 函数；移动 `mobile/core/state.js` 删演示版 logout；桌面/移动 index.html 加载顺序改为 state.js → api.js → engine.js；移动 index.html 删除 2 处 404 CSS 死引用（styles/brand.css、styles/motion.css，mobile.css 已自带 logo/login 类）+ 补 favicon。
- 自测：`node --check` 三文件通过；单测 124/124 通过（Suite F 桌面/移动共享纯函数漂移护栏仍 29 通过）；全站裸 fetch 收敛至仅 api.js 内部 3 处（login/apiFetch/logout）。
- 真机 e2e（Puppeteer 手机 viewport 375×812）：移动端登录通过——BM.apiLogin/apiSend/logout 全就绪、登录成功、token 就绪、pageErrors=0、consoleErrors=0；桌面端无回归——登录正常、roleLabel=系统管理员、pageErrors=0、4xx=0。**P0-2 彻底闭环。**
- 既有缺陷（非本轮引入，留待移动端独立产品线立项对齐）：移动 `mobile/data/data.js` 仅 4 角色（boss/manager/staff/finance），无 admin 等真实角色，真实登录后 curRole fallback boss（移动端 roleLabel 显示「总经理」）。

## 18:0x 确立「Sponsor 决策/仲裁边界 vs PM 自主边界」，固化入 TEAM.md §2.8
- 松哥澄清授权边界：非冲突类常规开发（含已拍板既定下一步）PM 直接做、事后汇报，不再逐次请示；Sponsor 只做决策 + 仲裁（需求冲突 / 设计冲突）；任务先后顺序不找 Sponsor（PM 更清楚）。
- PM 提出「必须找 Sponsor 6 类」清单，松哥拍板确认，并令写入治理规范。
- 落地：`governance/TEAM.md` 升 **V2.1**，新增 **§2.8**（找 Sponsor 6 类：①需求冲突 ②设计取舍 ③新增需求/理解偏差 ④范围扩张 ⑤对外/不可逆动作 ⑥纪律违规上报）+ PM 自主 5 类（排期 / 修 bug / 既定程序 / 测试 / 纪律前置动作）；V2.1 变更说明同步补记；`MEMORY.md` 补记该边界。

## 18:1x B 完成：三方评审汇总落《代码健康度评估》存档
- 按「先 A 后 B」+ 新边界（非冲突类既定程序直接做、不再逐次请示），把 PM 逐文件实证 + fe-dev + be-dev 三方评审汇总落成 `docs/代码健康度评估-2026-09-02.md`，作为 **D 线开发前基线**。
- 文档含：总体结论（三方定性一致 6.5/10「Demo 可用、工程化半途」）、健康度总览、6 条共识问题、fe-dev/be-dev 独有发现、亮点、P0 三件闭环状态、测试基线、后续 P1/P2 优先级 + D 线顺序建议（D5→D1→D4→D3/D2）。
- 实证数字核实：`server.js` 470 行、`modules/` 15 文件、`views/` 31 文件。
- **P0 三件全部闭环**（P0-1 错误中间件 / P0-2 统一请求层+移动端共享模块 / P0-3 工具收敛+esc 补引号）；P1（因子合一、数据权威单轨+闭环 TODO、版本化迁移、/api/orgs scope 裁剪、README 修正）/ P2（server.js 内联路由收编、mobile calc 层、视图加载去顺序强耦合）留待后续任务。

## 16:4x 确立「双映射文件维护」要求，TEAM.md 升 V2.2
- 松哥新增两条维护要求（口误「设计与代码映射」→ 自我纠正为「产品功能与代码映射」）：
  1. **开发工程师**每完成一个任务，更新《产品功能与代码映射关系》文件；
  2. **PM** 维护《需求-产品功能-模块对应关系》文件；
  3. **PM 每天汇报时**报告这两个映射文件的变化。
- 落地：`governance/TEAM.md` 升 **V2.2**，新增 **§2.9「产品功能-代码 / 需求-功能-模块 双映射维护」**（含维护者/更新时机/映射内容/维护要求/每日汇报）；§2.4 PM 职责补「映射维护」、§2.6 步骤 4「报告」补「更新产品功能与代码映射文件」、§3.3 每日同步规则补「每日映射汇报」。
- 两个映射文件（均建在项目根目录，初始基线 2026-09-02）：
  - `产品功能与代码映射关系.md`（开发工程师维护）：功能 → 前端视图/后端模块/核心层/移动端 四组映射表。
  - `需求-产品功能-模块对应关系.md`（PM 维护）：需求（V3 D1–D5 已拍板 + P1–P6 待议 + §0 纲领）→ 产品功能 → 模块 三级追踪 + 需求全量编号索引。
- 分工边界：PM 文件管「需求→功能→模块」自上而下追踪；开发文件管「功能→代码」自下而上落点；两者经「产品功能」中间层对齐，不得各记一套。

## 18:3x 松哥纠偏：需求跟踪表不全，PM 须全局补全
- 松哥指出《需求-产品功能-模块对应关系.md》只覆盖 V3 增量决策（D/P），不是项目全量需求；要求 PM 以全局意识，通过文档+对话+代码补全需求与产品功能。
- 全量盘点资料：V1（初稿）/ V2（全量需求主体，会议纪要确认版）/ V3（增量）/ 产品设计稿V1（M1-M11 模块 + D1-D6 决策）/ 软件设计架构分析V1 / 角色-界面映射分析 / demo-design v0.5/0.6 / 一期实施计划V1 / TASK_BOARD（M1-M18 已完成里程碑）。
- **需求三层来源确认**：① 一期三目标 T1-T3（V2 §1.2 纲领）；② 功能需求 F1-F11（V2 §5 全量主体）；③ 增量决策 D1-D5/P1-P6（V3）。
- **产品功能维度 = 设计稿 M1-M11**（M1 组织权限/M2 主数据/M3 编制/M4 版本平衡/M5 碰撞协商/M6 规则引擎/M7 AI诊断风险/M8 执行跟踪/M9 决算基线/M10 审计证据/M11 Copilot），非 TASK_BOARD 的 M1-M18（那是开发里程碑）。
- 重写映射文件为全量版：新增 §一 需求全景、§二 M1-M11 核心映射总表（含落地现状 ✅/⚠️/❌）、§三 F1-F11 正向索引、§四 一期三目标、§五 D/P 决策、§六 明确不做 8 项、§七 待确认项、§八 落地现状总览。
- **落地现状结论**：M1-M11 中 6 全实现（M1/M2/M3/M4/M6/M8）、5 部分实现（M5/M7/M9/M10/M11）；T1/T2 主体完成、T3 部分；D5 改动最小、P6 为新增业务规则最优先讨论。

## 18:5x 松哥问「下面还需我决策什么」，PM 收敛为三决策 + 落决策材料
- 松哥按边界问「还需要我决策什么」。PM 分类：①需拍板（P6 滚动预算、角色模型重构、P1 小类口径、AI 落地深度、P2-P5 轻量优化）；②需松哥牵头向客户要（D1 基础数据、D2 费控 API、7 待确认项）；③PM 自主（D5 备注栏、工程债排序、排期）。
- 松哥令「follow yourself」→ 执行 PM 建议：把最关键三决策各写一页决策材料。
- 产出 `docs/decisions/2026-09-02-待拍板决策-P6滚动预算-角色重构-P1口径.md`（decisions 目录已存在为空，正好落决策记录）：
  - **决策一 P6 滚动预算** → 推荐 A（一期不做、明确排除，留二期）：一期三目标不含滚动、数据模型大改、5 待确认点未闭环。
  - **决策二 角色模型重构** → 推荐 B（渐进补齐关键角色）：先补最缺的「归口责任人」+ 拆分集团三层，7 类基层先合并为「费用责任岗」；11 职能中心清单客户未供数会卡 A 方案。
  - **决策三 P1 小类填报** → 推荐 A（按最细经济事项填报，小类作聚合展示层）：与 V2「最细事实源」共识一致，才能多口径派生；需跟邬亮确认"小类"具体所指。
- 附拍板记录表，松哥逐条勾选 A/B/C。

## 19:0x 松哥拍板 + 澄清滚动预算口径
- **拍板结果**：决策二 角色模型 = **A 完整重构 14 角色**（松哥否了 PM 推荐的渐进 B）；决策三 P1 = **A 按最细经济事项**（同意 PM 推荐）。
- **P6 松哥澄清口径（关键更正）**：滚动预算 = 「一个年度内上一期（月/季度）预算未用完结转下一期、下期可用额度增加、全年总额不变」——是「期间结转」，**不是**「滚动预测」。PM 此前误读为滚动修订全年预算，已致歉更正。
- 据此 P6 重新评估：落地为 M8 执行跟踪的可用额度计算（本期可用=本期预算+上期结转），改动落 pure-calc.js 纯函数，不涉数据模型迁移；待定 3 小点（结转粒度/科目范围/上限）。
- 决策文档 `docs/decisions/2026-09-02-待拍板决策-*.md` 已回填拍板记录 + 决策一重写为月度结转口径；`MEMORY.md` 记入滚动预算定义、角色模型 A、P1 A 三条永久约定。

## 19:0x P6 三个小点拍板，三决策全部闭环
- P6 小点拍板：**按月结转 / 全部科目可转 / 不设上限**。
- 三个决策全部拍板完成：P6 月度结转（挂 M8，可用=本期+上期结转，落 pure-calc.js）、角色模型 A（完整重构 14 角色）、P1 A（按最细经济事项）。
- 决策文档 status 更新为已拍板 3/3；需求映射同步 P1/P6「待议→已拍板」，待议决策从 6 减为 4（P2/P3/P4/P5）。

## 19:1x 松哥确立「无冲突既定工作直接做 + 两渠道交付」，PM 产出角色重构设计文档
- 松哥批评过度请示：「没有冲突、只是重大工作，你们去做就好，为啥非要我点头；给我的东西走两个渠道——①发邮件 ②选择弹框，上下文文字不一定看」。
- 跨项目记忆 `~/.workbuddy/MEMORY.md` 追加：交付/汇报走邮件或弹框；无冲突既定工作直接做、不请示。
- 现状实证（读代码）：后端 `ROLE_SEEDS` 已有 14 个 role code，但是「新旧混合」——5 个 demo 遗留（boss/finance/staff/manager/buHead）+ 语义漂移（adminHead 前后端 desc 不一致）+ 职责重叠（boss↔ceo、finance↔companyBudgeter/cooAnalyst、staff↔expense、manager↔adminHead）。
- 产出 `docs/plans/2026-09-02-角色模型重构14角色设计.md`（草案待定稿）：收敛为 9 个 role code（+18 实例参数）+ 删 5 冗余角色 + 权限矩阵 + 视图差异化 + 迁移方案 + T1-T7 任务拆分。
- TASK_BOARD.md 登记 R1-R7 角色重构任务，更新「下一步建议」置顶角色重构。

## 19:2x 松哥拍板「按方案定稿开工」，角色重构设计定稿
- 松哥经选择弹框拍板「按方案定稿开工」：设计文档 `docs/plans/2026-09-02-角色模型重构14角色设计.md` 转 ✅ 已定稿，TASK_BOARD R1-R7 转「已定稿·开发中」。
- 下一步：交开发工程师（后端 T1-T2 角色收敛+权限闸门；前端 T3-T6 字典/状态闸门/切换器/视图差异化），完成后测试工程师 T7 回归 E2E。

## 20:0x 角色重构单测收尾：state.test.js 旧角色断言同步，全站回归全绿
- 修 `tests/unit/state.test.js` 残留旧角色断言 8 处（boss→ceo、finance→cooAnalyst、manager→adminHead、staff→expense），同步 canEditBaseData/canEditAccounts/canEditOrg/canViewBenchmark/scopedApprovals 等闸门测试语义与测试名。
- 结果：`test:unit` 122/2 → **124/0 全绿**（Suite B 32/32）；`backend-api` 20/0、`role-migration` 13/0、`db-interface` 12/0 全绿。
- 至此 **T1–T5 全部完成**（后端角色收敛/权限闸门 + 前端字典/状态闸门/切换器登录）。
- 排查 T6/T7 剩余：① 桌面端 `website/views/` 11 文件 ~50 处旧角色死分支（boss/finance/manager/staff/buHead）待语义迁移清理；② `wb-home` 差异化首页待设计（设计文档 §5 仅概述）；③ `data.js` ownerRole 字段（staff/manager）需迁移；④ E2E 断言旧 code 下线未跑。mobile 端是独立 demo（四角色），不在本轮范围（已有记录「留待移动端独立产品线立项对齐」）。

## 20:1x T6a 桌面端死分支清理完成 + smoke 冒烟修复，全站测试全绿
- 派前端工程师清理桌面端 views/ 11 文件 ~50 处旧角色死分支（boss→ceo、finance→cooAnalyst/companyBudgeter、manager→adminHead、staff→expense、buHead→组织视角），`data.js` ownerRole 字段迁移 expense/adminHead（部门 id `finance` 正确保留）。
- PM 验证（trust but verify）：views/core/data/app.js 无旧角色 code 残留，test:unit 124/0 全绿。
- 顺手修 2 个 smoke 冒烟脚本的历史遗留缺陷：① load 清单补 `core/utils.js`（BM.money 定义，M18 utils.js 拆分遗漏）；② smoke_kanban admin roleViews 断言补 ai-config；③ smoke_dom DOM 桩补 setAttribute/document.body/querySelector。smoke_dom/smoke_kanban 均通过。
- 全站回归全绿：unit 124/0、backend-api 20/0、role-migration 13/0、db-interface 12/0、smoke_dom/smoke_kanban 通过。
- 遗留：① 5 个语义待核对点（approval canAct 边界 / workbench 直达面板 / dashboard 组织树维度 / collision-tune 注释口径 / README 权限矩阵，均低风险视图显隐+文档细节，保持子代理合理推断）；② T6b wb-home 差异化待设计（涉及角色责任叙事+差异化视觉，需松哥引导）；③ T7 E2E 待测试工程师。

## 21:0x F1 角色切换器接线修复 + T7 E2E 22/0 + T6b 草案产出
- **F1 修复（bug 自主处置）**：测试工程师发现 `views/roleSwitch.js` 是孤儿文件——从未被 `index.html` 加载、`app.js` 无 `BM.switchRole`，角色切换器运行时不可达（逻辑已收敛、接线缺失）。PM 自主修复：
  - `index.html` 加 `<script src="views/roleSwitch.js">`；`app.js` 实现 `BM.switchRole(selected, params)`（演示通道重建 user.roles、写回 centerId/expenseType/scopeCompany、saveState、刷新标签+导航+回 wb-home）；`refreshRoleLabel` 改用「当前激活角色名」（原固定 user.roles[0].name，多角色切换后标签不准）；roleLabel 绑定点击入口 + title；`base.css` `.role-label` 加 cursor:pointer/hover。
  - 新增回归 `website/tests/smoke_roleswitch.js`（11/0），复用 smoke 桩模式（补 parentNode）。
- **T7 回归 E2E**：`role-convergence.e2e.cjs` **22/0 全绿**（角色字典 9 code、切换器 9 卡+参数联动、5 seed 用户映射、roleViews 白名单+越权负断言）。
- **T6b 草案**：产出 `docs/plans/2026-09-02-工作台差异化首页T6b设计草案.md`（🟡 待定稿）——9 角色 wb-home 差异化（scopeText/roleHint/roleTips/专属面板四维），按 scope 分组（group/company/center/self/all），复用现有视图+组件，不新建 6 个待建视图；末尾 3 个待拍板点（面板密度/总览卡数据源/roleTips 口径）。
- 验证全绿：unit 124/0、smoke_dom/smoke_kanban/smoke_roleswitch、role-convergence E2E 22/0。

## 21:1x T6b wb-home 差异化首页草案定稿
- 产出并定稿 `docs/plans/2026-09-02-工作台差异化首页T6b设计草案.md`（✅ 已定稿）。
- 松哥经选择弹框拍板 3 决策点：① 面板密度 = 每角色 2~3 块；② 总览卡数据 = **接真实表**（`unit_budget`/`budget_execution`，复用 `/api/unit-summary` 等 M10 查询，非 mock）；③ roleTips = **预留动态接口**（Copilot 按角色 + 实时数据生成，不写死静态文案）。
- 设计要点：9 角色 wb-home 差异化（scopeText/roleHint/roleTips/专属面板四维），按 scope 分组（group/company/center/self/all），复用现有视图+组件，不新建 6 个待建视图（归口工作台/压降下达/协商谈判区/masterData/auditTrail/copilot）。
- T6b 待开发（交开发工程师实现 workbench.js + data.js；后端仅现有接口汇总粒度不足时补只读汇总端点）。

## 21:4x T6b wb-home 差异化首页开发完成 + 全量回归绿
- **后端 T6b-1**（已提前完成）：`server/modules/budget-compile.js` 新增 `workbenchOverview(db, orgIds, months)`（真实表 `unit_budget`+`budget_execution` 聚合 `{totalBudget,totalExec,remain,execRate,units,topOverspent}`，超预算科目按 cat 聚合 exec>amount 降序取前 5）；`server/server.js` 新增 `GET /api/workbench-overview`（auth + resolveAllowedOrgIds 定范围）；`backend-api.test.cjs` 加 2 用例（admin 全域结构断言 + expense 受限仍 200）。
- **前端 T6b-2**：`data.js` `ROLE_HINTS["wb-home"]` 补全 9 角色文案（消除 cooLead/legalHead/companyBudgeter/centerOwner 四角色兜底错配成 cooAnalyst 的问题）。
- **前端 T6b-3**：`workbench.js` `renderHome` 重构为 5 层骨架（①hello 9 分支责任叙事 `scopeTextFor` ②roleHint ③预算业务提醒 ④scope 分组专属面板 `renderScopePanels` ⑤AI 关注）。总览卡 `renderOverviewCard` 调 `/api/workbench-overview` 接真实表（kpi 4 指标 + 超预算 TOP）；`renderEntryCards` 按角色入口卡（ceo/cooLead/cooAnalyst/legalHead/adminHead/companyBudgeter/centerOwner/admin 各 1~2 块，与总览卡合计 2~3 块）；expense 走 `renderExpensePanels`（我负责的项目+报销提示，沿用既有）；admin 走 `renderPlatformOverview`（用户/角色/组织统计，接 `/api/users|roles|orgs`）。roleTips 改 Copilot 动态接口 `renderRoleTips`（`/api/copilot/ask` 传 `roleFocusQuestion(r)` 按角色生成，`aiEnabled:false`/异常降级占位「AI 未启用」）。旧静态 `roleTips` 函数删除。
- **测试 T6b-4**：新增 `website/tests/smoke_workbench.js`（40/0，vm 沙箱验 scopeText 9 角色互异无错配 + entryCardsFor 视图目标 + roleFocusQuestion + formatTipsAnswer + renderHome 9 角色零异常）+ `tests/e2e/wb-home-diff.e2e.cjs`（21/0，真机 Puppeteer 验 ceo/expense/admin/centerOwner/adminHead 五代表角色 hello 标题、总览卡 kpi、专属面板、AI 关注区、全程无 pageerror）。
- **全量回归全绿**：unit 124/0、smoke_dom/smoke_kanban/smoke_roleswitch/smoke_workbench、backend-api 22/0、role-convergence E2E 22/0、wb-home-diff E2E 21/0。
- 设计文档 §8 验收勾选全完成、TASK_BOARD R1-R7 转「T1–T6b 全部完成」。**待松哥确认是否 commit + 版本号 +1 + tag + push（未擅自提交，本轮改动横跨 T1–T7 全量，含 F1/T6b 前端 + T6b 后端）**。

## 21:4x 松哥授权执行版本纪律，v1.4.0 已提交并推送
- 松哥令：「工作纪律执行，不要问我，问你的项目经理」→ 授权 PM 自行执行 git 提交（不再逐次请示）。
- 执行结果：`git add -A`（84 文件，+3833/−977，范围核对无 db/log/密钥混入）→ `commit a56d770`（feat: 14角色收敛重构+工作台差异化首页T6b）→ `git tag v1.4.0`（1.3.5→1.4.0）→ `push origin main --tags` 成功（`74fc3f8..a56d770`，新 tag v1.4.0）。
- 说明：本轮 T1–T7 全量 + F1 + T6b 一次综合提交（已累积为单一变更集，无法按模块拆分），符合「最小位 +1、打 tag 后 push origin/main」纪律。本地与远程 HEAD 一致（a56d770）。

## 21:5x 查证 3 个原始 Excel 里「经济事项多级分类」——有，且是现成分类树
- 松哥令：找最早给的 3 个 Excel，看里面有没有「经济事项」的多级分类。3 文件均在 `docs/product/`。
- **结论：有**，且是可直接落库的现成分类树，分两套口径：
  - **① 总经办口径**（`2.预算科目及逻辑-总经办表单.xlsx`，Sheet「1.预算逻辑」145 条）：列 = 管理责任部门→预算科目名称→**费用类型名称**（多级，`-` 分隔）→经济事项说明→预算逻辑。费用类型 2 级 137 / 3 级 1 / 1 级 7。部门=行政/外联/环安/后勤/总办其他。示例「办公费-修理费-自主维修」(3级)。
  - **② 费控口径（最完整）**（`各中心费控系统...表单.xlsx`，Sheet「费控系统预算科目明细表」353 条）：列 = 会计科目编码→**会计科目名称**（自身 1~4 级，`-` 分隔）→所属分类名称→费用类型名称→预算科目名称→预算归口部门→编码。层级 1级5/2级158/3级150/4级40。一级大类 ~21 个（人工/工会经费/残保金/办公运营费/外部服务/税费/材料消耗/能源消耗/安全生产/其他费用/财务费用/主营业务成本/营业税金及附加/营业外支出/其他…）。4 级示例「人工-人事费-中介服务费-普通中介费用」。另有「Sheet3」费用类型名称清单 347 条（如「差旅费-商旅机票费」）。
  - **③ 汇总表**（`3.2026年总经办预算汇报-汇总表`）：纯金额填报，11 个「预算关键要素」分 sheet，无分类树。
- **与 P1 决策呼应**：P1「按最细经济事项填报」的最细口径 = 文件2 费用类型名称（353 条）+ 文件1 费用类型名称（145 条），可作系统经济事项分类字典的数据源。

## 22:1x 经济事项 4 级分类基础数据重构（松哥令，方案 A 拍板，派三 agent 闭环）
- 松哥令：经济事项基础数据从 1 级 → 4 级（文档是 4 级、系统只有 1 级且种子仅 6 个），重构数据结构 + 页面，安排相关 agent。
- 现状实证：`economic_event` 1 级平铺（seed 6 个）；`account_subject` 早有 `parent_id` 字段但从未启用；前端 `basedata.js` 平铺表格。
- **拍板**（松哥）：方案 A（account_subject 升级 4 级树，economic_event 挂科目节点）+ 立即开工，PM 自行推进不再请示。
- **设计文档** `docs/plans/2026-09-02-经济事项4级分类基础数据重构设计.md` 定稿。
- **后端**（sanan-backend-dev）：account_subject 加 level/path 建 4 级树；抽取脚本 `scripts/extract_subject_tree.py` 从 2 Excel 抽 `server/seeds/subject-tree.json`（树 146 节点 L1=18/L2=64/L3=58/L4=6 + 叶子经济事项 300）；`GET /api/subjects?tree=1` 返回树、平铺加 level/path；`seedSubjectTree/seedEventLeaves` 幂等。新增 `tests/integration/subjects-tree.test.cjs`(9/0)。
- **前端**（sanan-frontend-dev）：`basedata.js` 平铺改 4 级可折叠树（renderSubjectTree）+ 级联表单（optgroup 列全部科目节点）；新增样式 `.bd-tree-*`。
- **测试**（sanan-qa-tester）：写方案 `docs/2026-09-02-经济事项4级分类测试方案.md`(42 用例) + 执行；新增 `seed-integrity.test.cjs`(10/0)、`basedata_tree.e2e.cjs`(10/0 真机)；修 3 个陈旧 E2E（p02 选择器/rules_event_map 科目数+标题/smoke DB_FILE+token）。全量零回归：单测 124/0、smoke 4/4、集成全绿、E2E 全绿。
- **语义修正（关键）**：文档真实语义「费用类型挂到对应会计科目节点（叶子或中间均可）」，全库仅「物料消耗」1 条挂在中间节点「材料消耗-物料消耗」(L2，另有 9 个 L3 子科目)。方案 A 原「挂叶子」是过度简化，已放宽并同步改前端/测试断言/设计文档。
- **遗留 major（不阻塞）**：科目数不稳定——全新库 152（146 树+6 样例）vs 开发库 206（146 树+60 历史平铺残留），根因 `migrateSubjects` 不清旧平铺科目。待后续清理旧平铺科目（涉及删 demo 数据，需谨慎）。
