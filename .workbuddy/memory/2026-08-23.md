# 2026-08-23 工作日志

## 三安项目 · 概念对齐：预算控制的方法「是什么 / 放在哪」
- 松哥强调：9 种还是 6 种不重要；关键是厘清该概念的**本质与系统位置**。
- 结论（与松哥对齐）：
  - **是什么**：上面（管理层/集团/中心负责人）给每个经济事项预先挂的**控制规则**（确定性公式→控制基线金额），基层不可选/不可改，只对照报、说明。
  - **位置（三层）**：① 主数据/配置层——挂在经济事项上的配置属性，填报前由上面定义；② 流程层——属"管理线下达"侧，是基层填报的前置输入，位于"单数据源·双派生视角"管理线一端（与指标/压降同族）；③ 界面层——基层屏只读（带"上级定义"标签），真正定义/维护在上方的管理配置模块。
  - 一句话定位：管理线下达控制规则→生成控制基线→卡基层申报；基层只管申报额+偏离原因。
- 待松哥确认是否把"定义与定位"写入设计稿 V1（加一节）。本回合仅对齐，未改文档/代码。

## 三安项目 · 范围再平衡（导航砍到 3 + 工作台首页作导航+提醒 + 对标仅上级）
- 松哥 03 轮定稿：
  1. 横向比较只能给更高一级的领导或部门 → `BM.canViewBenchmark(roleId)` 谓词门禁，仅 boss/ceo/finance/cooLead/cooAnalyst 可见对标。
  2. "按这个思路改 + 主要功能就三点 + 去掉无关功能 + 去掉工作台" → 松哥随即修正：保留工作台首页作「功能导航 + 预算业务提醒窗口」；3 个功能：① 新预算编制 ② 预算看板（含总揽/执行进展/偏差预警）③ 财务规则（各级财务部门）。
  3. 对标/决算/风险合并入看板对应面板（总揽/执行进展/偏差预警），不再单占导航。
- 落地：roleViews 收敛为 [wb-home, compile, kanban, rules]；新建 kanban.js 组合 dashboard/benchmark/track/final/risk-view（零重复逻辑）；首页 workbench.js 重写为 3 张核心功能导航卡 + 预算业务提醒；dashboard.js/final.js 自渲染回调改回渲到传入容器；CSS 加 kb-* 看板样式 + wn-title/wn-sub 导航卡样式。
- 验证：node --check 9/9、test_calc 16/16、smoke_dom 12/12；新建 smoke_kanban.js 4/4（boss 含对标面板 / expense 锁定 / 谓词 / 导航收敛）；8123 HTTP 200；Playwright 截图 3 张视觉验证。
- 交付：docs/dev-report-2026-08-23-rebalance.md + output/rebalance-2026-08-23-*.png。
- 服务：发现 8123 掉了（无人监听），用 `python3 -m http.server 8123 --bind 127.0.0.1` 后台重启（任务 iKPv5Y）。
- 未尽：① V1 是否加"对标权限门禁"节 ② 后端 agent 启动 ③ track/final/risk-view/dashboard 文件未删是否归档。

## 三安项目 · 页面打磨轮（报告冻结，先做页面）
- 松哥指令：报告先不动，先把页面弄出来、页面满意后再回头改报告。
- 打磨 1：看板内嵌 5 个子视图页头冗余（预算总览/横向对标/预算追踪/决算/风险筛查各自 page-head，与 kb-sec-head/kb-subhead 三级堆叠）→ CSS `.kb-sec-body .page-head{display:none}`，由面板标题统一承担（computed 验证 5/5 隐藏，innerText 减 264 字符）。
- 打磨 2：编制页标题"编制工作台·2026"→"新预算编制 · 2026"（对齐导航口径）。
- 回归：node --check、smoke_dom 12/12、smoke_kanban 4/4 全绿；真机 DOM 复查导航 4 项正确。
- 待松哥过目页面给意见，满意后再回改报告。

## 三安项目 · 基层编制工作流 5 块落地（正式页面开发）
- 松哥定义基层员工预算季业务流：① 工作台收预算编制通知（文本可浏览 / 消息旁挂开始编制按钮）② 编制页按"我负责的经济事项"筛选 ③ 每行「听取 AI 建议」（读政策/往年/执行 → 建议区间）④ 申报值滑块微调 ⑤ 月度拆解二级页（图形化、总量守恒）。
- ⑤ 图型拍板：方案 B —— 双堆叠条对比（上条=上年实际执行占比·锁定基准，下条=本年度·可拖分隔线此消彼长·总量恒 100%），弃柱状图与折线（对比图形语言需一致）；差异标注 ±%、一键套用上年分布、金额可精确输入自动冲正。
- 术语映射（松哥定）：demo「项目」= 客户「经济事项」（~390 项，费控表字段"具体经济事项"，单一事实源）；demo「项目视角」= 客户「管理视角」（11 职能中心归口）。
- 落地文件：data.js（RULE_OWNERS 负责人归属 / myRules 筛选 / aiSuggestion 确定性建议 / baseMonthlyRatio+decomposeByRatio 月度模式 / CTRL_METHOD_ASSIGN 补全 6 个规则事项控制方法）；compile.js（范围筛选默认"我负责的"、申报值滑块+数值、听取 AI 建议行内展开、月度分解入口按钮）；monthly-split.js 新建（双堆叠条+pointer 拖动+守恒+保存写回草稿）；workbench.js 通知卡+开始编制按钮；app.js/index.html 注册 monthlySplit 视图。
- 修复：CTRL_METHOD_ASSIGN 原只覆盖 8 个科目 id，6 个规则事项全落 history 兜底 → 补齐（manageStd/volume/qtyPrice/manual），控制基线=规则基线。
- 验证：node --check 8 文件、test_calc 16/16、smoke_dom 12/12、smoke_kanban 5 项（含 renderMonthlySplit）、Playwright 真机全流程（①通知=1+按钮=1；②行=4 我负责的；③AI 展开区间 314.3~366.7 万；④滑块=4；⑤二级页 12 段/11 分隔线/合计=控制基线 3492000、拖动前后守恒）。
- 交付：output/flow-2026-08-23-*.png 4 张 + 预览。报告继续冻结。

## 三安项目 · 编制主表简化为 8 列（松哥截图定稿）
- 背景：松哥连发 8 次「方案B」后附图：原全功能表（规则/方法/基线/申报/偏离/原因/月度分解行/AI展开）信息太杂，要"去掉没意思的"。
- 定稿 8 列：① 经济事项 ② 会计科目（acctCode）③ 上年预算（lastBudget mock=决算×1.1）④ 上年决算（lastYear）⑤ 偏差（决算−预算：超支红/节支绿）⑥ 本年度预算值（滑块+数值，保留 ④）⑦ 月度拆分按钮（方案B 二级页入口）⑧ AI 建议（区间+采纳中值，依据悬停 title）。
- 去除：规则 expr、控制方法标签+生成基线按钮、控制基线列、偏离状态、偏离原因、月度分解 12 格/迷你条、AI 展开行。
- 数据：BM.RULES 加 lastBudget + acctCode（6602.11~15/99 mock）。
- 落地：data.js、compile.js（thead+renderRows 8 列、保存/提交适配、去掉 reason/month-input 依赖）、views.css（ai-range-compact）。
- 验证：node --check、smoke_dom 12/12、smoke_kanban 5 项、真机 8 列（4 行/滑块4/拆分4/采纳4/旧残留0/零报错）。截图 output/flow-2026-08-23-compile-8col.png。

## 三安项目 · 编制表模块化（前后台 + 数据库）落地
- 业务背景（松哥）：正式客户项目；每年编制一次、平时只看；数据量不大；用户分布全国各地 → 本地先开发模块、部署另议（松哥选 3）。
- 技术栈拍板：Node 22 + Express（workspace 已装）+ 内置 node:sqlite（--experimental-sqlite，零原生依赖）；**单端口一体化**（8300 同时页面+API，取代 8123 静态 + CORS）。
- 新增 server/：db.js（建表 economic_event + 6 条种子 + decomposeMonthly/aiSuggestion/compileBaseline 纯函数与前端口径一致）、server.js（静态 + 4 API：health/events/events:id/amount/monthly + 偏差查询时算）、package.json、README.md；economic_event.db 运行时生成。
- 列序定稿（松哥）：经济事项 / 会计科目 / 本年度预算值 / 月度拆分按钮 / 上年预算 / 上年决算 / 偏差 / AI 建议（8 列）；去掉角色筛选（"先不考虑用户角色"）。
- 前端对接：compile.js 数据源优先 fetch /api/events（apiMode），失败 fallback mock（buildMockList）；滑块/采纳走 persistAmount（PUT amount），月度拆分保存走 PUT monthly；数据源状态条（在线/离线）。monthly-split.js 保存 API 模式 PUT monthly。
- 验证：API 冒烟（6 条/守恒/400/404）✓；E2E 真机（改值→PUT→DB=3000000→刷新读回=3000000→月度拆分拖动保存→DB monthly 和=3000000 守恒、零报错）✓；smoke_dom/smoke_kanban 全绿。
- 启动：cd server && NODE_PATH=... node --experimental-sqlite server.js → http://127.0.0.1:8300/?as=expense#compile
- 交付：output/flow-2026-08-23-module-8300.png。报告仍冻结。

## 三安项目 · 编制页收敛为两个视角（松哥定稿）
- 指令：编制页只留「经济事项视角」+「财务会计视角」；去掉预算控制方法说明、上面的总结表、下面的独立填报表、压降试算（"压价全部去掉"）。
- 落地：8 列填报表（事项/科目/预算值/月度拆分/上年预算/上年决算/偏差/AI建议）并入「经济事项视角」；「财务会计视角」= 按会计科目聚合（本年度预算值合计，aggBy 改基于 src）；删除编制状态条、方法 chips、dualHint、center/both 视角、独立填报表块、压降试算块；update 过时注释。
- 验证：node --check、smoke_dom/smoke_kanban 全绿；8300 真机（两视角按钮=2、经济事项 8 列 6 行 6 滑块、财务会计 6602.11→114万、方法 chips=0、压降=false、零报错）。截图 output/flow-2026-08-23-compile-two-views.png。

## 三安项目 · 模块二：上级部门汇总工作流（消息收件箱 → 组合汇总 → 压降 + AI 质检 + 注释）
- 松哥定稿：① 上级部门工作台收"管辖单位预算编制完成"消息（下级单位数按组织结构自动确定，不写死）② 每条可展开看填报（与基层一致）③ 打勾多选→组合汇总 ④ 部门级汇总做压降（**两个维度**：事项行 × 单位列，叠加）⑤ AI 报告质检（较上年决算 ↑/↓）⑥ 注释编辑 = 因素分析，**存数据库**。
- 数据库扩展（server/db.js）：organization 表（单位树，HQ + 2010/2020/2170/3050 四单位）+ unit_budget 表（org×cat：amount/monthly/last_budget/last_year/method/ai + reduce_ratio/reduce_amount/note），种子按单位规模系数生成。
- API 扩展（server.js）：GET /api/orgs、GET /api/unit-budgets?org=、GET /api/unit-summary?orgs=（按事项汇总+较上年±%）、PUT /api/unit-budgets/:id/reduction（压降+注释存库）。
- 前端：views/unit-inbox.js（收件箱：单位消息卡+展开只读8列表+勾选+组合汇总按钮）、views/unit-summary.js（矩阵：单位×事项、行/列压降滑块叠加、每格点击弹窗压降+注释、AI 质检面板带注释保存）；首页加"部门预算汇总"入口卡；app.js startView 放行非 roleViews 已注册视图（#unitInbox/#unitSummary 可 hash 直达）。
- 验证：API 冒烟（orgs 4 单位、unit-budgets 守恒、summary vs去年±%、reduction+note 存库 174600）；E2E 真机（收件箱 4 消息→勾选 2→组合汇总 6 事项×2 单位+8 滑块→AI 质检 6 项→注释写库 1 条；零 pageError）；smoke 全绿。截图 output/flow-2026-08-23-unit-summary.png。
- 增补（松哥）：部门汇总表每个下级部门旁加"调整滑块（0~20%）+ 调整原因输入"（us-reason，change 存库到该部门所有事项 note）→ 验证 2 部门滑块+原因、存库 6 条。截图 output/flow-2026-08-23-unit-col-adjust.png。

## 三安项目 · 预算看板重构三视图 + 决算与偏差独立模块
- 松哥指令：预算看板即总览（不要"预算总揽"面板）；看板三个视图：财务会计 / 经济事项 / 人员；把"年度决算"和"偏差"移出看板放新模块。
- 看板（kanban.js 重写）：三视图切换（account 按科目聚合 / event 事项明细 7 列 / person 按责任人聚合，新增 BM.EVENT_OWNERS+PEOPLE+aggByOwner）；去掉预算总揽/执行进展/年度决算/偏差预警/对标面板。
- 新模块（final-risk.js）：年度决算（renderFinal）+ 偏差预警（renderRiskView）+ 横向对标（renderBenchmark，仅上级可见，expense 锁定）三面板；VIEWS 注册 finalRisk、index.html 加载、首页"决算与偏差"入口卡、#finalRisk 直达。
- 修 bug：compile.js initEventsApi 的 finally 原无条件 openView("compile")（任何视图调它都会被拽回编制页）→ 改为仅当前在编制页时重渲。
- 验证：看板三视图（人员视图：李静114万/王敏538.2万/张伟180万/赵磊48万/周芳0）、finalRisk boss 三面板+对标完整（8 子公司/均值93.4万/偏离判定）、expense 对标锁定、零报错；smoke 全绿。截图 output/flow-2026-08-23-kanban-3views.png + flow-2026-08-23-final-risk-boss.png。
- 模块三（松哥要求）：组织架构 + 用户账户 + 角色系统完整开发。
- server/db.js 增 4 表（role/user/user_role/session，scrypt 密码哈希）+ organization 加 level 深化为三级树（HQ→4 公司→12 部门）；
- server.js 加 auth 中间件 + 8 API（auth login/logout/me、roles、orgs/tree、users CRUD）；
- 前端 login.js 改正式账号密码登录 + 6 演示账号一键填 + 演示通道；state 扩 user/token；
- roleViews 改按用户角色 views 取并集；roleSwitch 仅列已分配角色（打开时刷新 me）；
- app.js 加 hashchange 监听 + 退出登录；
- 新增 org.js（组织树全员只读）+ accounts.js（admin 账户管理）。
- 回归全绿、8300 HTTP 200；真机 E2E 全通（admin 登录→组织架构 17 节点→账户管理→新建用户→新用户登录→403 拦截→多角色切换仅显示已分配）。
- 过程中修真 3 bug：① ?as=admin 触发演示通道覆盖真实 token（改用 hash 切换）；② accounts.js nameInput/userInput 未设 type="text"（显式设 type）；③ BM.ROLES 缺 admin 角色（加定义）。
- 截图 6 张 output/auth-2026-08-23-*.png。报告继续冻结。
- 模块三·增补（松哥要求）：登录窗口演示账号按组织层级分组（集团层/事业部层/公司部门层/基层层，11 账号）；组织树迁移为「HQ → 行政服务事业部(BU-ADM,business) → 4 公司 → 12 部门」+ 集团职能节点（FIN-HQ 集团财务部/COO-HQ 总经办/ADM-HQ 行政服务中心）；新增角色 buHead 事业部负责人（孙悦）；上下级部门示例：集团财务部（李静·上级）↔ 一公司财务部（王敏·下级）。
- 修真 3 处：listOrgs 缺 level 字段致收件箱单位空（补查 level）；孙悦 user_role 旧 legalHead 迁移为 buHead；userToDto roles 查询无排序（加 ORDER BY ur.rowid，李静主角色 finance 在前）。
- 验证：4 组 11 演示账号、5 个层级账号登录全通（孙悦·事业部负责人/周芳·职能中心/李静·财务经理/王敏·部门经理/赵磊·基层）、组织树 21 节点、收件箱 4 公司、零报错。截图 output/auth-2026-08-23-login-groups.png + org-v2.png + sunyue-buhead.png。

## 三安项目 · 基础数据管理模块（B1–B7 全交付 · 2026-08-23 晚）
- 松哥指令「现在开发数据基础管理的页面」→ 完成独立「基础数据管理」页（顶部「基础数据」入口 `#basedata`），内部双 Tab：经济事项 | 会计科目；规则版本管理保持独立「预算规则」页（不并入）。
- 数据模型：`account_subject` 独立表 + `economic_event.subject_id`；`migrateSubjects()` 按 acct_code 去重生成 60 科目并回填 subject_id（幂等）。
- 后端：`/api/subjects`、`/api/events` 全 CRUD + `requireBaseDataEditor`（admin/finance/cooLead/cooAnalyst/centerOwner）；被引用科目删除拦截。
- 前端：`website/views/basedata.js`（双 Tab+表格+增删改模态+只读提示）+ `views.css` 配套；`app.js` 注册、`state.js` 加 NAV_LABELS/roleViews 回退/canEditBaseData、`db.js` ROLE_SEEDS 5 角色补 basedata 视图 + 对已存在库角色行幂等迁移、`index.html` 引入。
- **修真 bug**：`createSubject` INSERT 列名与取值错位（code 误存 name、name 误存 cat…），导致手动建科目字段全错位；已修正，回归验证 code/name/center/method/sortNo 正确。
- E2E：`scripts/e2e_basedata.py`（Playwright）12/12 通过——导航可见性（admin 有 / staff 无）、双 Tab 渲染、科目+事项 CRUD、基层直链只读。修真：renderEventsTab/renderSubjectsTab 重渲染未清 body 致重复表格（after=163=81+82），已加 `body.innerHTML=""`。
- 导入脚本 B7：`import_excel_data.py` 改为先建 account_subject（按 acct_code 去重、幂等、不破坏手动科目）再回填 economic_event.subject_id；重跑验证 subjects 恒 60 不翻倍。
- 验证：/api/subjects=60、/api/events=81 且全部带回填 subjectId（0 mismatch）；5 个特权角色视图均含 basedata，staff 不含。服务运行于 8300。
