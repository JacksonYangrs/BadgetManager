# 2026-08-26 工作日志

## 单元测试体系建立 + 前后端口径漂移修复（BadgetManager）

- 建立单元单测套件 `tests/unit/`（harness.js + run.js + calc/state/engine/backend-consistency 四套），共 **72 通过 / 0 失败**；既有 `website/tests/test_calc.js` 16/0 无回归。
- **修复 P1 真实 bug**：前端 `calc.js` 与后端 `db.js` 的 `decomposeMonthly` 默认权重数组口径漂移（前端相对权重和≈12 vs 后端百分比和=1.0，比例不同 → 同年度额月度分布不一致）。
  - 抽 `server/pure-calc.js`（无副作用纯函数 `MONTHLY_WEIGHTS` + `decomposeMonthly`）；`db.js` 改为 `require("./pure-calc")`；`calc.js` 默认权重提升为命名常量 `BM.MONTHLY_WEIGHTS`（同字面量）。
  - 移动端 `index.html` 引入 `../core/calc.js` 复用同一内核（消除第三份实现隐患）。
- **P2a**：`state.js roleViews` 真实登录分支补 BASE 兜底（`compile/kanban/rules` 始终可见）+ BD 角色补 `basedata`、`admin` 补 `accounts`，与演示通道对齐。
- **P2b**：Suite D 从源码契约升级为真实跨端校验（`require` 后端 pure-calc + 前端 calc 比对逐月分布），并锁死移动端引用。
- **P3**：移动端引入 calc.js + 新增 `tests/integration/backend-api.test.cjs` 与 `tests/e2e/smoke.e2e.cjs`（缺 express/puppeteer 时优雅 SKIP，不阻断 CI）。
- 复现：`node tests/unit/run.js`；集成/E2E 需先 `npm i express` / `npm i puppeteer`。

## 后续三项建议落地（"进行这些建议"）

三项后续建议全部实施并转真实绿（单元 124/0 + 集成 7/0 + E2E 3/0）：

### ① 装依赖跑通集成/E2E（SKIP → 真实绿）
- `npm i express puppeteer`；`package.json` 固化 express^4.19.2 / puppeteer^25.9.0（CI 可复现）。
- 集成层（7/0）：health/auth/events 回归 + 新增 pure-calc 合同断言（PUT 测试额→后端月度分布===pure-calc.decomposeMonthly，验证后还原不污染库）。
- E2E 层（3/0）：自包含起服 `node --experimental-sqlite server/server.js`（E2E_PORT||8401）+ 真实无头 Chromium，断言 P2a 核心入口（wb-home/compile/kanban/rules + accounts/basedata）+ 前端/后端 decomposeMonthly 实时合同。
- 坑：puppeteer v24+ `executablePath()` 返回 Promise，需 `.then` 解析；Chromium 缓存在 `~/.cache/puppeteer/chrome/mac_arm-<ver>/...`。

### ② 移动端 engine.js 副本收敛 + 跨端漂移护栏（Suite F）
- diff 实证 `website/mobile/core/engine.js` == 桌面 `core/engine.js`（字节一致）→ 删移动端副本，`index.html` 改引 `../core/engine.js`。
- `mobile/core/state.js` 与桌面 `core/state.js` 属不同应用域（视图键/数据模型/登录模型不同）→ 判定不可合并，否则破坏移动端导航。改以护栏替代：新增 Suite F（vm 沙箱加载移动端 state.js，比对共享纯函数 money/fmtMoney/pct/uid，29/0）把漂移转可捕获。

### ③ 规则基线口径对账 → 抽 pure-rule.js + Suite E
- 新增 `server/pure-rule.js`（HARDCODED_FACTORS / EXEC_RATE / compileBaseline / aiSuggestion 纯函数，可单测）；`db.js` 删本地副本改转发并注入 RULE_FACTORS。
- 新增 Suite E（23/0）：锁前端 applyRule/compileByMethod vs 后端 compileBaseline/aiSuggestion（因子 down5 0.95 … actual 1.0 一致）。
- 约定：据实类无历史 → 前端基线 null、后端经 createEvent 的 lastYear||0 回退 0（占位，非因子漂移），已文档化。
- 改造后回归确认全绿（db.js 重构安全）。

### 收尾
- 已重写 `reports/UNIT_TEST_REPORT.md` 反映 124/0 + 7/0 + 3/0、Suite E/F、pure-rule.js、engine.js 去重，三项建议标记完成。

## UI 调整：去掉桌面端"切换角色"和"重置 Demo"

- 按截图要求移除桌面顶部栏"切换角色"、"重置 Demo"两个按钮，保留"退出登录"。
- 同步移除 `website/index.html` 中对 `views/roleSwitch.js` 的引用。
- 移除 `website/app.js` 中的 `BM.switchRole`、`BM.refreshNav` 及两个按钮的点击监听。
- 保留 `BM.resetState`（移动端仍用）和 `views/roleSwitch.js` 文件（仅不再加载）。
- 服务仍在 8300 运行，预览面板可刷新看到效果。

## 导航重构：预算看板→预算跟踪、预算规则→预算规划、费控导入入口

- `website/core/state.js`：`BM.NAV_LABELS` 中 `kanban` 改为「预算跟踪」，`rules` 改为「预算规划」；相关注释同步更新。
- `website/views/kanban.js`：页面标题改为「预算跟踪」；在页头右上角新增「📥 费控导入」按钮，点击打开既有 `importView`（已支持 Excel 导入）。
- `website/views/rules.js`：页面标题改为「预算规划」，副标题说明「预算规则是预算规划的核心内容」；权限提示同步。
- `website/views/basedata.js`、`website/data/data.js`：把用户可见的「预算看板」「预算规则」提示文字同步改为「预算跟踪」「预算规划」。
- 未动移动端（用户截图与需求均为桌面端）。

## 导航重构后续修正

- 「费控导入」最终落地：从左侧菜单移除（已从 `state.js` 的 `roleViews` BASE 数组删除），改为「预算跟踪」页右上角按钮（kanban.js 新增 `📥 费控导入`），点击通过 `BM.openImportModal()` 弹出二级子页面（modal 包裹 `import-view.js` 的 `renderImportView`，非嵌入、非菜单）。
- 「新预算编制」全局改名为「预算编制」（`NAV_LABELS`、compile.js 页面标题、monthly-split.js 空态提示）。

## UI 修正：预算编制「财务会计视角」科目名称列取真实科目字典

- 问题：财务会计视角新增的「科目名称」列显示的是科目编码数字（6600010124 等），因为原实现仅用 `BM.RULES` 的简化编码（如 6602.11）兜底， 与后端真实 `acct_code` 不匹配。
- 修复：`website/views/compile.js` 的 `renderAccountView()` 现在异步调用 `/api/subjects`（带 `BM.authHeaders()`），用返回的 `code → name` 映射填充「科目名称」；查询失败或离线时回退到 `BM.RULES` 的经济事项名称。
- 新增 `BM.subjectCache` 避免反复请求；首次切换「财务会计视角」会显示「正在加载科目字典…」。

## 归档目录去重（archive/documents 冗余归并）

- 核验：`archive/documents/website-docs/` 含 v0.1–v0.5（5 份），`archive/documents/demo-design-versions/` 仅 v0.1–v0.4 且与前者逐字节相同（md5 一致）；v0.5 仅存于 website-docs。
- 按松哥确认「删除」：移除 `demo-design-versions/` 下 4 份副本 + 清空目录本身；保留 `website-docs/` 完整 5 份。删除前已确认内容在 website-docs 完整保留，无数据损失。

## 权限中间件收敛（server/middleware/auth.js）

- 收敛前：auth 在 server.js / expense-import.js / policy_rules.js 各抄一份，requireBaseDataEditor 双份，requireFinance 与 requireRuleEditor 重叠。
- 新增 `server/middleware/auth.js`：导出 `buildAuth(dbm, db)` 工厂（auth 需 db 解析 token；requireXxx 仅依赖 req.user），角色集合集中。
- 改造：server.js / expense-import.js / policy_rules.js 改为 `const { auth, ... } = buildAuth(dbm, db)`，删除内联副本。
- 验证：集成 7/0（含 /api/auth/login）、db-interface 12/0、单元 124/0，全绿。

## 归档退役代码清理（archive/retired-code）

- 松哥确认删除 `archive/retired-code/`（旧移动端 retired-code/desktop-mobile-legacy/mobile/*，12 个文件，无任何代码引用，已被 `website/mobile/` 取代）。
- 已 `rm -rf archive/retired-code` 并核验：`archive/` 现仅剩 `README.md` + `documents/`（website-docs 5 份）。
- 注：archive 为项目目录（非个人目录），删除前向用户明确确认目标与范围，内容已确认无现役引用，风险可接受。

## db.js God Object  解构收尾 + 接口回归（M18 完成）

- 拆分方案见 `docs/architecture/module-breakdown-2026-08-26.md`；实现：9 业务模块（`server/modules/*`）+ AI 三件套（ai-gateway 占位 / ai-budget-decision / expense-import 迁入），`db.js` 退化为组合根，公开 API 不变，`aiSuggestion` 下沉 ai-budget-decision（内部依赖，不进根）。
- **验证结论**（本段接续）：
  - 定位并修复 `website/core/state.js` 演示通道 BASE 误含 `importView`（与 389 行注释矛盾），导致 `roleViews('staff')` 多出一个菜单项 → 移除后 `roleViews` 回归 32/32。
  - 新增 `tests/integration/db-interface.test.cjs`：锁定组合根按引用合并全部 91 个符号（12/0）。
  - 全套：单元 124/0、集成 API 7/0、接口回归 12/0、e2e 3/0，全绿。
- **关键结论**：`aiSuggestion` 已从 db 根导出移除，grep 确认无外部调用方引用 `dbm.aiSuggestion`，仅内部模块使用，零风险。
- 复现：
  - `npm run test:unit`（124/0）
  - `npm run test:integration`（7/0， 含 /api/health）
  - `node --experimental-sqlite tests/integration/db-interface.test.cjs`（12/0）
  - `npm run test:e2e`（3/0，需 puppeteer）

## 模块设计文档（每个模块一份 · MODULE_SPEC）

- 按松哥要求「每个模块有自己的设计文档（依赖 / 输入输出 / 业务处理流程）」，在 `docs/architecture/modules/` 生成 13 份：
  organization / auth / subjects / events / budget-compile / budget-execution / rules / ai-policy-extract / notifications / ai-gateway / ai-budget-decision / expense-import / middleware-auth。
- 模板：职责(单一) / 是否独立 / 依赖入 / 依赖出 / 共享资源 / 输入输出 / 业务处理流程 / 解耦措施 / 关键决策规则（遵循 module-structure-discipline 的 MODULE_SPEC）。
- 与 `docs/architecture/module-breakdown-2026-08-26.md` 同目录（「跟架构设计放在一起」= 同目录，无需移动）。

## MODULE 文档写法纠正（重要 · 跨项目通用）
- 松哥反馈：模块设计文档「写得太技术」，要求「把业务说清楚」——每份必须先用大白话讲清「模块在业务上干什么、给谁用、业务流程」，技术接口/表/函数仅作后置「速查」。
- 已按「业务视角优先」重写 13 份（一句话定位 → 业务说明 → 业务视角流程 → 技术速查 → 依赖解耦 → 决策规则）。
- **应固化进 module-structure-discipline 技能**：MODULE_SPEC 必须含「业务定位 + 业务说明 + 业务视角流程」，技术细节后置，不能只列 API/表名。

## 安全/健壮性审计修复（2026-08-26 晚）

松哥做了 7 项审计，逐条确认并修复（Craft 模式直接改）：

### 修复项与改动文件
1. **未鉴权接口（P0）**：`server/server.js` 给 GET/PUT /api/events、GET /api/events/:id、GET /api/orgs、GET /api/orgs/tree、GET /api/roles、GET /api/unit-budgets、PUT /api/unit-budgets/:id/reduction 加 `auth`；写入类加 `requireBaseDataEditor`。
2. **数据范围控制（P1）**：`server/middleware/auth.js` 新增 `resolveAllowedOrgIds(dbm, db, user)`，按 scope（all/group→全域；company→本公司及下级；center→归口中心；dept/self→本人）推导可访问组织集合；在 /api/orgs、/api/unit-budgets、/api/unit-summary、/api/executions 强制过滤。scope 语义是模型此前在 auth.js ROLE_SEEDS 设计的，已固化，buHead 归 group（如需「事业部=本公司」可后续调整。
3. **测试/开发库隔离（P1）**：`.gitignore` 加 `server/economic_event.db`；`server/db.js` 支持 `DB_FILE` 环境变量，测试用独立临时库。
4. **政策上传（P1）**：`server.js` 的 MARKITDOWN 改读环境变量 `MARKITDOWN_BIN` 回退默认；`runtime/uploads` 加 `mkdirSync(recursive)`。
5. **月度守恒（P2）**：`server/modules/events.js` 的 `updateMonthly` 增加 `sum(monthly)===amount` 校验，不一致返回 400。
6. **执行校验（P2）**：`server.js` PUT /api/executions 校验 month∈[1,12]、amount≥0，否则 400。
7. **测试准入（P2）**：`tests/integration/backend-api.test.cjs` 启动失败改 `exit(1)`；并带登录态、用独立 DB。

### 过程中发现的既有 bug（已修）
- `server.js` 启动顺序：原 `seedBuCodes` 早于 `initAuth`（后者才加 organization.type 列），导致**新库启动崩溃**；已把 `dbm.initAuth(db)` 移到 `dbm.seedBuCodes(db)` 之前。

### 验证
- 集成测试 7/0（含登录态）；单元 124/0；db-interface 12/0；手动验证基层账号越权→403、未登录→401、admin→200、月度守恒→400。
- 重要：`resolveAllowedOrgIds` 必须放进 `buildAuth` 返回对象（否则 server 报 is not a function）。

---

## 21:50 基于 Sanan logo 的网站 UI 审计
- 对 http://localhost:8300/ 执行设计/SEO/AI 可见性综合审计，产物：`reports/audit/report.html`、`reports/audit/capture.json` 及截图。
- 核心结论：当前 Demo 主色 #1E2A4A 与 logo 蓝 #003CB4 脱节；页面无 h1/h2/h3、无 robots/sitemap/JSON-LD/llms.txt；移动端顶栏标题截断。
- 给出 A/B/C 三个 redesign 方向（品牌校准、精准感放大、克制动效身份），并附设计 Token 建议。
- 工具降级说明：未安装 stardust/impeccable/marketing-skills/refero，采用 Puppeteer + curl 手工完成。

## 22:05 Redesign 实现（方向 A · 仅视觉令牌+logo）
- 松哥拍板：方向 A 品牌校准，范围=仅视觉令牌+logo（不含 SEO/LLM 骨架）。
- 设计文档：`docs/plans/redesign-sanan-brand.md`。
- 改动：`website/styles/base.css`+`mobile/styles/mobile.css` 主色改为 #003CB4 族；新建 `website/styles/brand.css` 存 Sanan logo data URI 与 `.brand-mark`/`.login-logo` 背景；`index.html`/`mobile/index.html` 加 brand.css 引用、theme-color 改蓝、顶栏 SVG 换 logo 背景；`views/login.js`+`mobile/views/login.js` 登录页 logo 区改背景。
- 真机验证（Puppeteer）：桌面/移动顶栏主色 rgb(0,60,180)=#003CB4，`.brand-mark` 背景为 logo data URI（logoShown=true）。截图 `reports/audit/assets/after-*.png`。
- 未做：语义 h1/h2、robots/sitemap/JSON-LD/llms.txt（下一轮）；B/C 方向未实现。

## 22:10 配色刻度 + 控件微动效重构
- 松哥要求：重构主色/配色刻度 + 控件元素动画效果（在 A 方向基础上）。
- 设计文档：`docs/plans/redesign-motion.md`。
- 改动：`base.css`/`mobile.css` 增加 --c-primary-tint/--c-primary-soft/--c-disabled/--c-disabled-bg 与 --t-fast/--t-base/--t-slow 过渡令牌；新建 `website/styles/motion.css`（按钮 hover 上移+阴影、active 按压、卡片 hover 浮起、导航 active 金条滑入、输入框 focus 环、登录卡 loginIn、消息 msgIn、弹窗 modalIn、Toast 已有、并尊重 prefers-reduced-motion）；`index.html`/`mobile/index.html` 接入 motion.css。
- 真机验证：桌面/移动 .btn 过渡生效、.login-card animation=loginIn 0.4s、主色仍为 #003CB4；截图 `reports/audit/assets/after-motion-*.png`。

## 22:30 Tab 激活态与按钮主色统一
- 修复问题：松哥反馈"tab 组切换色彩不统一、按钮颜色好像也不统一"。
- 所有页面内 Tab 激活态统一改为 logo 蓝：`website/styles/v02.css .mt-btn.active`、`website/styles/components.css .rtab-btn.active` 与 `.scope-card.active`、`website/styles/v04.css .dash-tab.active`、`website/styles/views.css .bd-tab.active`（原已蓝，确认生效）、`website/mobile/styles/mobile.css .tab-btn.active .tab-ico` 与 `.nav-tab.active`。
- 按钮主操作色统一：`.btn-accent`（原金）重定义为 logo 蓝；`.role-btn.active`（原金）改为蓝；移动端 `.btn-accent` 金渐变改为蓝。
- 金色 `--c-accent` 仅保留：① 顶部全局导航当前位置指示条（`.qn-btn.active::after`，深蓝栏点睛）；② 状态徽标/关键数值/成功高亮。
- 量化验证：Puppeteer 登录后遍历 6 个主导航 view，`.btn`/`.btn-accent`/`.btn-primary` 中 `backgroundColor = rgb(201, 164, 74)` 金色残留数 = **0**。
- 真机验证：基础数据页 `.bd-tab.active` 计算色为 `rgb(0, 60, 180)` = `#003CB4` 蓝；登录页"登 录 →"按钮显示为蓝。
- 真机截图：`reports/audit/assets/unify-bdTab.png`、`unify-desktop-login.png`。设计文档见 `docs/plans/redesign-motion.md` 第 5 节。

## 21:50 文件选择按钮美化
- 修复问题：松哥反馈"还有几个文件选择按钮很丑"。
- 定位到两处裸用原生 `<input type="file">`：`website/views/rules.js`（预算规则→创建明年新规则→导入政策文件）、`website/views/import-view.js`（费控 Excel 导入）。
- 统一成可复用 `.file-picker` 组件：原生 input 隐藏（保留可访问性），外层 `<label class="btn btn-primary file-pick-label">选择文件</label>` 触发，右侧 `.file-picker-name` 显示已选文件名。
- 样式落到 `website/styles/components.css`；复用 `.btn-primary` 自动跟 logo 蓝统一规则一致，不再散落原生控件。
- 真机验证（Puppeteer）：登录后分别进入 rules（切到"创建明年新规则"）和 importView，`.file-pick-label` 计算色 `rgb(0, 60, 180)`（#003CB4 蓝）、白字；原生 input 已隐藏（1px/absolute/clip）；真上传临时文件后文件名正确回显。
- 真机截图：`reports/audit/filebtn-rules.png`、`reports/audit/filebtn-import.png`。设计文档见 `docs/plans/redesign-motion.md` 第 6 节。

## 22:55 顶部栏：消息铃移到退出登录左边
- 按截图要求，`website/index.html` 的 `.topbar-right` 子元素顺序从「铃铛 → 角色标签 → 退出登录」改为「角色标签 → 铃铛 → 退出登录」。
- 真机验证：DOM 顺序 `roleLabel, bellBtn, logoutBtn`；截图 `reports/audit/topbar-bell-order.png`。

## 22:59 预算跟踪页：费控导入按钮去掉前导 icon
- 按反馈，`website/views/kanban.js` 的「📥 费控导入」按钮改为纯文字「费控导入」。
- 真机验证：预算跟踪页右上角按钮文字为 "费控导入"，无 icon；截图 `reports/audit/kanban-import-btn.png`。

## 23:10 预算规则「适用经济事项」布局重排
- 按截图要求，将 `rules.js` Tab3 从"左规则卡 + 右科目勾选"改为"上中下"三段式：顶部规则卡横向滚动条、中间注释文本框（带动效）、底部经济事项勾选。
- 改动：`website/styles/components.css`（`.evt-map` 垂直 flex、`.evt-cards` 横排滚动、`.evt-comment` 动效）+ `website/views/rules.js`（DOM 三段式、`setCommentValue`/`animateCommentSwitch`）。
- 注释框内容按规则卡本地缓存，点击卡片时 180ms fade+位移切换；规则说明默认取自 `BM.RULE_EVENT_MAP` 的政策表述或规则基线逻辑。
- 真机验证：规则卡 `overflowX=auto`，点击第二张卡注释框正确切换，经济事项网格在下；截图 `reports/audit/rules-events-layout.png`、`rules-events-card2.png`。
- 23:21 微调：注释框标签「规则说明 / 注释」移到框内左侧，与输入共占一行；框高从 80px 减到 40px。
- 23:23 修正：`website/views/kanban.js` 的「费控导入」按钮原带 `btn-sm`，比其他按钮小；已去掉 `btn-sm`，现在为 `.btn.btn-primary` 标准尺寸（height 32px / padding 7px 14px / font-size 13px）。
- 23:25 微调：去掉规则卡片内的说明文字（`.sc-l`），只保留标题和分类徽章；注释框内标签与输入文字统一 `line-height: 20px` + `padding: 0`，保证垂直对齐。
- 23:26 修正：预算规则「当前版本」Tab 里，小标题「控制基线比例（相对上年决算）」改为「规则卡片」；规则卡片的英文 scopeKey（down5/canteen/dorm…）改为中文名称（总经办归口，刚性压降 / 按人数核定，超线压降 / 阶梯压降 / 挂钩营收…）。
- 23:31 微调：注释框 flex 对齐从 `align-items: center` 改为 `align-items: baseline`，让标签与输入文字在同一基线对齐。
- 23:35 微调：注释框改回 `align-items: center`，并把 `textarea` 换成 `contenteditable` div，使标签与输入文字在框内上下居中，不再靠上。
- 23:55 预算编制表调整：
  - 把「月度拆分」列移到表格最后一列（原第 4 列 → 第 8 列）。
  - 「月度拆分」按钮改为 `.btn-primary`（蓝色填充），更醒目。
  - 「采纳中值」按钮改为 `.btn-outline-primary`（蓝色边框、透明填充、蓝色文字），与其他按钮区分。
  - 新增 `.btn-outline-primary` 样式到 `website/styles/base.css`。
  - 改动：`website/views/compile.js` + `website/styles/base.css`。
  - 真机验证：表头顺序正确；月度拆分按钮 `backgroundColor = rgb(0, 60, 180)`；采纳中值按钮 `backgroundColor = rgba(0,0,0,0)`，`color = rgb(0, 60, 180)`，`borderColor = rgb(0, 60, 180)`；截图 `reports/audit/compile-table-reordered.png`。
- 00:07 登录页 logo 替换：
  - 按截图要求，把登录页原先的金色占位方块（`.login-logo`）换成 Sanan 三安制造 logo。
  - 改动：`website/styles/v02.css` 的 `.login-logo` 改为使用 `var(--logo-sanan)` 背景图，尺寸 120×48px，透明底。
  - 真机验证：`backgroundImage` 为 Sanan logo data URI，`backgroundColor` 透明；截图 `reports/audit/login-logo-sanan.png`。
- 设计文档：`docs/plans/redesign-motion.md` 第 7 节。
