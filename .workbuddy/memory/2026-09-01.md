# 2026-09-01 工作日志 · BadgetManager

## 14:5x 修复：部门预算汇总（unit-summary）误报「后端未启动」
- 松哥报「AWS 服务崩了」，前端提示「加载失败：后端未启动（请运行 server/ 模块）」。
- 排查结论：**AWS 服务未崩**——badgetmanager.service active 已跑 4 天、公网 `/api/health` 200、登录/events/unit-summary/orgs-tree 全 200。
- **根因**：`website/views/unit-summary.js` 三处 `fetch` 漏带 `BM.authHeaders()`（`saveReduction` PUT / `GET /api/orgs` / `GET /api/unit-budgets?org=`），后端返回 401 → 前端 `units.forEach` 抛异常 → 落 `.catch()` 误报「后端未启动」。与 8/27 修 unit-inbox 同类，当时漏修了此文件。
- **修复**：三处 fetch 补 `headers: BM.authHeaders()`。
- **部署**：rsync `unit-summary.js` → AWS `/opt/badgetmanager/website/views/` → 公网验证文件已更新（3 处 authHeaders）、health 200、`/api/orgs` 带 token 返回 127 单位、`/api/unit-budgets` 结构正确。
- **版本**：commit `1109558` · tag **v1.3.3** · push origin/main。
- **关键运维信息（跨会话必记）**：AWS EC2 `3.25.82.99`（ap-southeast-2），部署目录 `/opt/badgetmanager`，systemd `badgetmanager`（node22 + `--experimental-sqlite`，端口 8300），SSH 私钥 `~/AIProjects/awaconfig/my-OPC-app-key.pem`（用户 ubuntu）。数据表：`organization` 127 company + 16 dept + 1 group(HQ)，`unit_budget` 正式公司每单位 81 条。

## 15:3x 收件箱无数据单位灰标 + 卡片紧凑化（v1.3.4）
- 松哥反馈：①收件箱单位卡片太高（"那条框变短"）；②勾选单位组合后无数据。
- **数据排查**：数据没丢，是勾选了演示简版单位（`BU-00` 股份 / `BU-01` 氮化镓…共 85 个）本身无 `unit_budget` 数据；有数据的正式版是 `1000`/`2010`…共 42 个（每单位 81 条）。`organization` 表混两套数据。
- **松哥拍板选项 A（两个都 A）**：加灰标 + 卡片紧凑化。
- **改动**：
  - `budget-compile.js` 新增 `orgBudgetFlags()`（org_id → 记录数）
  - `server.js` `/api/orgs` 给 units 加 `hasBudget` 字段
  - `unit-inbox.js` 无数据单位加 `no-data` 类 + 副标题「暂无预算数据」
  - `v02.css` 新增 `.todo-item.ui-unit` 覆盖（padding 8px14px、td-ico 28px、td-sub margin-top 1px）+ `.no-data` opacity 0.55
- **部署**：rsync 4 文件 → AWS → 重启 `badgetmanager` → 公网验证 `hasBudget` 正确区分（42 有数据 / 85 无数据）。
- **版本**：commit `8ceefe5` · tag **v1.3.4** · push origin/main。

## 15:39 收件箱过滤无数据单位 + 网格布局（v1.3.5）
- 松哥反馈：①无预算数据的不要显示（不只是灰标）；②排列改一下，少几行方便选。
- **改动**：
  - `unit-inbox.js`：收到 units 过滤 `hasBudget=true` 才渲染（42/127），box 容器加 `unit-inbox-grid` 类
  - `v02.css`：新增 `.unit-inbox-grid`（`display:grid repeat(3,1fr) gap 10px`；`@media` 900px 降 2 列、560px 降 1 列）
  - 边界：所有单位都无数据 → 空提示「暂无可汇总的下级单位（所有单位暂无预算数据）」
- **设计权衡**：后端 `/api/orgs` 不动（`unit-summary.js` 还要拿完整 orgMeta，过滤会缺 code→name 映射）。
- **部署**：rsync 2 文件 → AWS（前端静态，无需重启）。
- **版本**：commit `74fc3f8` · tag **v1.3.5** · push origin/main。

## 16:4x 沉淀三方评审反馈 → 需求调研稿 V3
- 松哥贴了三方评审反馈（邬亮·功能与业务规则 / 杨红梅·功能与数据对接 / 杨若冰·会议协调与部署），选「沉淀成需求稿 V3」。
- **现状核对结论（关键）**：费控导入入口、跟踪页 AI 预警/偏差归因、后勤按人头核定公式（分层规则方案）**已有**；真正净缺口 6 项——①填报备注栏（compile.js `reason` 字段硬编码空串、UI 缺输入框）②跟踪页按超标/执行率排序 + AI 管控建议（track.js 有预警无排序）③预算明细表导出（编制页无导出按钮）④月度总结报告推送（消息模块 M6 有、报告生成未落地）⑤**滚动预算（全新业务规则，全项目无，须先讨论 5 个待确认点）**⑥专网部署 + 最高权限移交（部署形态变更，当前 AWS 公网）。
- 产出：`docs/product/三安光电AI费用预决算管理系统需求调研稿V3.md`（增量升级稿，与 V2 合读；含 §6 现状映射总表、§8 待确认清单、§7 相对 V2 修订索引）。
- 门禁提醒：滚动预算属新增业务规则，按「讨论—设计—开发」须先对齐再进设计。

## 16:5x 关键决策纪要 → V3 升级为「决策确认版」
- 松哥贴了正式《关键决策》纪要（标题「确定预算系统后续的基础数据提供及核心功能调整安排」），是上轮三方反馈的权威版。
- **松哥拍板**：①更新 V3 为决策确认版；②滚动预算「保留为待确认项」（不降级不移出）。
- **正式决策 5 条（D1–D5）**：D1 对方提供准确基础数据（经济事项明细/组织架构/**历史年度预算**，新增项）；D2 对方协调确认费控 API 接口（用于预算跟踪自动同步）；D3 专网部署+与费控同环境；D4 交付后对方最高权限、**开发方无访问权限**；D5 填报页备注说明栏。
- **待议 6 条（P1–P6）**：按经济事项小类填报 / AI 管控建议 / 跟踪页排序 / 明细导出 / 月度报告推送 / 滚动预算。
- **关键变化**：①填报维度从「大类-小类-部门」收敛为「按经济事项小类填报」；②新增反馈人「用户250178」（负责基础数据+费控API确认）；③权限补强「开发方无访问权限」。
- V3 已重写为「决策确认版」，正文按「已拍板 D1-D5 / 待议 P1-P6」二分，§6 现状映射总表含决策状态列。

## 16:4x A 线 P0-2 blocker 修复 + QA 复测通过（先 A 后 B · 进行中）
- 背景：P0 三件代码加固（P0-1 后端 errorMiddleware / P0-2 前端统一请求层 / P0-3 工具收敛+esc）已完成开发，QA 回归报 **blocker**：`compile.js`（预算编制视图）3 处裸 fetch 未走统一层，其中 `GET /api/events`、`PUT /api/events/:id/amount` 不带认证头，真实登录后 401 → 视图降级「数据源·离线」本地 mock、保存走草稿而非数据库。
- **修复**：fe-dev 将 compile.js 3 处裸 fetch 改为 `BM.apiGet`/`BM.apiSend`（统一层 `state.js:366` 自动带 Bearer + 401 触发 handleSessionExpired）。仅改 compile.js，未触碰其他文件。
- **QA 复测（真机 Playwright）通过**：4 用例全过——①数据源在线（81 行=后端行数、0 个 401）②PUT 生效（改 792098→2026665 读回一致，已还原）③财务会计视角 /api/subjects 正常（60 行真实科目名）④P0-1/P0-3 无回归。
- **重要发现（待松哥拍板范围）**：P0-2「统一请求层」实际**未完全收口**——全站仍残留约 35 处带 `BM.authHeaders()` 的裸 fetch 未走统一层（basedata.js 10、rules.js 14、app.js 5、data.js 2、accounts.js 2、unit-inbox.js 1、mobile/app.js 1）。这些带认证头、**不 401、功能正常**，但不享受统一层的「401 统一会话过期跳转 + 统一错误处理」。compile.js 那种「不带认证头→401 降级」的才是真 blocker（已修）。
- 建议：残留裸 fetch 记为 P0-2 遗留（低风险、功能正常），A 判定完成，先推进 B（三方评审落《代码健康度评估》基线文档）；收口作独立迭代。待松哥定。
