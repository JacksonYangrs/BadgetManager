# 产品功能与代码映射关系

> 维护者：**开发工程师**（前端 sanan-frontend-dev / 后端 sanan-backend-dev）
> 更新时机：**每完成一个任务后**（并入 `governance/TEAM.md` §2.6 步骤 4「报告」）
> 性质：产品功能点 ↔ 代码实现的单一真源映射；PM 每日汇报其变化
> 初始基线：2026-09-02

---

## 维护规则

1. 每完成一个任务，在向 PM 报告代码位置的同时，更新本文件——新增 / 修改的「功能点 ↔ 代码」映射条目，不得遗漏、不得滞后。
2. 表格按「产品功能」分组；一个功能对应多个文件时全部列出。
3. 若发现本表与现实代码不符（漂移），顺手修正，保持单一真源。
4. 移动端是独立产品线（不随桌面端交付），单独分组，不混入桌面端。

---

## 一、桌面端 · 功能 → 前端视图 → 后端模块

| 产品功能 | 前端视图（`website/views/`） | 后端模块（`server/modules/`） | 说明 |
|---|---|---|---|
| 工作台首页 | `workbench.js` | —（前端聚合） | 角色差异化入口 |
| 预算编制 | `compile.js`、`monthly-split.js` | `budget-compile.js` | 自上而下 / 自下而上双路径 |
| 预算跟踪 / 看板 | `kanban.js`、`dashboard.js`、`track.js`、`unit-summary.js`、`unit-inbox.js` | `budget-execution.js` | 执行率、单位汇总、归口 inbox |
| 预算规划 / 规则 | `rules.js` | `rules.js` | 三类管控规则、版本化 |
| 预算调整 | `adjust.js`、`balance.js` | `budget-compile.js`、`rules.js` | 追加 / 压降 / 平衡 |
| 基础数据（含组织架构） | `basedata.js`、`orgchart.js` | `organization.js`、`subjects.js`、`events.js` | 科目 / 事项 / 组织可编辑 |
| 预算工作人员 | `accounts.js` | `auth.js` | 账户与角色分配 |
| AI 配置 | `ai-config.js` | `ai-config.js`、`ai-gateway.js` | 模型 / 网关配置 |
| AI Copilot | `copilot.js` | `copilot.js`、`copilot-retrieval.js` | 受控动态 SQL + 回灌作答 |
| 费控导入 | `import-view.js` | `expense-import.js` | Excel 导入 + 执行跟踪 |
| 决算 | `final.js`、`final-risk.js` | `budget-execution.js` | 决算确认 + 风险复核 |
| 审批中心 | `approval.js` | `budget-execution.js` | 审批链 |
| 决策中心 | `decisions.js` | — | 决策留痕 |
| 横向对标 | `benchmark.js` | — | 仅上级可见 |
| 碰撞 / 调参 | `collision.js`、`collision-tune.js`、`tune.js` | `budget-compile.js` | 申报差异协商 |
| AI 风险筛查 | `risk-view.js` | `ai-budget-decision.js` | 提示非判定 |
| 角色切换 | `roleSwitch.js` | `auth.js` | 多角色轻量切换 |
| 登录 | `login.js` | `auth.js` | 统一账户认证 |

## 二、桌面端 · 核心层（`website/core/`，共享内核）

| 模块 | 职责 | 被谁复用 |
|---|---|---|
| `api.js` | 统一请求层 + 认证 + 会话过期（`apiLogin/apiFetch/apiGet/apiSend/logout`） | 全站（桌面 + 移动） |
| `utils.js` | DOM 创建 `el` / 转义 `esc` / 金额 `money·fmtMoney` | 全站 |
| `state.js` | 状态核心：默认状态 + 本地持久化（`defaultState/loadState/saveState/resetState`） | 全站 |
| `access.js` | 查询与权限：预算访问 `getCat*` + 数据范围 `scoped*` + 角色视图 `roleViews` + 权限闸门 `canEdit*` + 登录 `login/curRole` | 全站 |
| `actions.js` | 业务动作：审批 / 建议 / 采购 / 调整 / 编制流程 / 规则 / 碰撞 / 报销 | 全站 |
| `calc.js` | 纯函数计算 | 前端各视图 |
| `engine.js` | AI 意图路由（本地降级） | `copilot.js` / 移动 copilot |
| `role-hint.js` | 角色提示 | 各视图 |

### 数据层（`website/data/`，2026-09-04 自原 `data.js` 拆分）

| 模块 | 职责 |
|---|---|
| `organization.js` | 组织 / 角色字典（`ORGS/DEPTS/ROLES/COMPANIES/BUSINESS_UNITS/ORG_TREE/SCOPE_LEVELS/...`） |
| `budget.js` | 预算科目 / 项目 / 物料（`CATEGORIES/SUMMARY/PROJECTS/MATERIALS/DEFAULT_RULES/...`） |
| `transactions.js` | 单据 / 审批 / 建议 / 风险流水（`DOCS/APPROVALS/SUGGESTIONS/RISKS/...`） |
| `rules-engine.js` | 客户规则引擎 + AI 编制建议（`RULES/applyRule/budgetAdvice/RULE_EVENT_MAP/DUAL_TRACK/...`） |
| `kanban.js` | 看板数据层（`loadOrgTree/loadKanbanData/kanbanAgg/orgChildren/...`） |

## 三、后端模块（`server/modules/`，15 个）

| 模块 | 职责 |
|---|---|
| `auth.js` | 认证 / 会话 / 用户角色 |
| `organization.js` | 组织架构（环检测 + 四道删除守卫） |
| `subjects.js` | 预算科目 |
| `events.js` | 经济事项 |
| `budget-compile.js` | 预算编制 |
| `budget-execution.js` | 预算执行 / 审批链 |
| `rules.js` | 预算规则 |
| `ai-budget-decision.js` | AI 预算决策（提示层） |
| `ai-config.js` | AI 配置 |
| `ai-gateway.js` | AI 网关（密钥加密） |
| `ai-policy-extract.js` | AI 政策抽取 |
| `copilot-retrieval.js` | Copilot 检索（白名单 + 脱敏） |
| `copilot.js` | Copilot 对话 |
| `expense-import.js` | 费控 Excel 导入 |
| `notifications.js` | 消息通知 |

> 另：`server/server.js`（Express 主入口）、`server/db.js`（组合根 + 迁移）、`server/pure-calc.js` / `pure-rule.js`（确定性纯函数）、`server/policy_rules.js`（因子实现，待收口为单一真源）、`server/middleware/`（鉴权 + 错误中间件）。

## 四、移动端（`website/mobile/`，独立产品线）

| 产品功能 | 前端视图（`website/mobile/views/`） | 复用内核 |
|---|---|---|
| 工作台首页 | `home.js` | 桌面端 `../core/{utils,calc,api,engine}.js`；state 用移动端自有 `core/state.js` |
| 预算 | `budget.js` | 同上 |
| 审批 | `approval.js` | 同上 |
| AI 助手 | `copilot.js` | 同上 |
| 登录 | `login.js` | `../core/api.js`（`apiLogin`） |
