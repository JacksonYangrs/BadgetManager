# 三安光电 AI 费用预决算管理系统 · 状态续报（截至 2026-08-23 00:00）

> 撰写：PM（Buddy）
> 收件：Sponsor（松哥）
> 性质：续报 — 承接 08-22 09:00 明早汇报的「三点提案」，本报到此已落地 + 验证，并补记昨夜概念对齐。
> 运行约定：浏览器 `http://localhost:8123/`（静态根 `website/`），`?as=<角色>` 指定角色，`#<视图>` 深链直达。

---

## 一、三点指示 — 现状：已全部落地 + PM 复验（DONE，非提案）

| # | 指示 | 落地动作 | 关键文件 / 证据 | 状态 |
|---|------|----------|----------------|------|
| ① | 视角索引：经济事项视角以「经济事项」为索引；财务视角以「会计科目」为首要 | `compile.js` 双视角重构为**按首要索引切换**：`event`（经济事项）/ `account`（会计科目 `accountCode` 聚合）/ `center`（职能中心）/ `both`（并排比对）；新增 `aggBy(keyFn)` 聚合助手，三视角同数据源派生 | `views/compile.js`：aggBy×4、dualModes 重构为四模式、默认 `renderDual("event")` | ✅ 已改 + 验证 |
| ② | 主工作台：编制是年度短窗口，日常是报销监控 / 跟踪 | 主工作台 = **运营监控中枢**（`buildMonitor`：待我审批 / 风险预警待复核 / 报销在途·项目预算 或 预算执行进度·超标预警）；「编制季」降级为窗口期卡片（非窗口期显示倒计时/只读预览）。`defaultView` 全角色统一为 `wb-home` | `app.js:31` `return "wb-home"`；`views/workbench.js` 运营监控中枢×5 / buildMonitor | ✅ 已改 + 验证 |
| ③ | 命名 + 归属：叫「预算控制的方法」，且由上面定义（非基层自选） | 全量改名 `COMPILE_METHODS`→`BUDGET_CONTROL_METHODS`；新增 `CTRL_METHOD_ASSIGN`（上级预挂到各事项）；`compile.js` 每行「编制方法下拉（自选）」改为**只读方法 + "上级定义"标签 + 生成控制基线按钮**；系统按上级定义方法生成控制基线并比对偏离 | `data/data.js`：BUDGET_CONTROL_METHODS×2、CTRL_METHOD_ASSIGN×1；`views/compile.js`：ctrl-method-ro×1、`BM.calc.compileByMethod(...)`.amount 生成基线；`core/calc.js` 语义改为"控制基线" | ✅ 已改 + 验证 |

> ③ 同时消除了原设计矛盾：`compile.js` 旧版让填报人每行**自选**方法（正是被否定的"每个人照着方法来编制"模型），现已改为上级预挂只读 + 生成控制基线。

---

## 二、验证总览（PM 复跑，全绿）

| 项目 | 命令 | 结果 |
|------|------|------|
| 语法检查 | `node --check` views/compile.js, app.js, views/workbench.js, data/data.js, core/calc.js | ✅ 5/5 OK |
| 确定性计算引擎 | `node tests/test_calc.js` | ✅ 16/16 通过 |
| 烟囱渲染/交互/计算 | `node tests/smoke_dom.js` | ✅ 12/12 零异常 |
| 本地预览服务 | `curl http://127.0.0.1:8123/` | ✅ HTTP 200（运行中） |

**结论**：三点均按你 23:35「去改」的授权实际落地，回归无回退。

---

## 三、概念对齐（昨夜 00:00 你点明的「是什么 / 放在哪」）

你强调：**9 种还是 6 种不重要；关键是厘清本质与系统位置。** 已与你对齐结论：

### 是什么
「预算控制的方法」= 上面（管理层 / 集团 / 中心负责人）给每个**经济事项**预先挂的**控制规则**（确定性公式 → 控制基线金额）。
基层**不可选 / 不可改**，只对照报「申报额」并说明偏离。一句话定位：**管理线下达控制规则 → 生成控制基线 → 卡基层申报**；基层只管申报额 + 偏离原因。

### 放在什么位置（三层）
1. **主数据 / 配置层**：挂在经济事项上的配置属性，填报前由上面定义 —— 对应 `BM.BUDGET_CONTROL_METHODS`（方法字典）+ `BM.CTRL_METHOD_ASSIGN`（事项→方法映射）。
2. **流程层**：属「管理线下达」侧，是基层填报的**前置输入**，位于「单数据源 · 双派生视角」的管理线一端（与指标 / 压降同族）。
3. **界面层**：基层屏**只读**（带"上级定义"标签），真正的**定义 / 维护**在上方的管理配置模块。

### 数量（9 vs 6）
按你的判断**不重要**，以管理层 / 客户实际定义为准；演示继续标注「待上面定义确认」，不替基层定。

---

## 四、待你拍板（唯一未决项）

**是否把「预算控制的方法：定义与定位」写入设计稿 V1（加一节）**，让 demo 与客户对齐口径？

> 草稿已备好（见下），你说一声「去改」我就落进 `docs/architecture/三安光电费用预决算-产品设计稿V1.md`，并同步映射文档。本续报仅汇报，**未擅自动 V1**。

### 拟写入 V1 的草稿

> **§X 预算控制的方法：定义与定位**
> 预算控制的方法，是管理层 / 集团 / 中心负责人给每个经济事项预先下发的**控制规则**（确定性公式 → 控制基线金额），用于约束与核对基层申报；基层不可自选、不可更改方法，只据实报「申报额」并说明偏离。
> 系统位置分三层：① 主数据层——挂在经济事项上的配置属性（`BM.BUDGET_CONTROL_METHODS` 字典 + `BM.CTRL_METHOD_ASSIGN` 映射），填报前由上方定义；② 流程层——属管理线下达侧，是基层填报的前置输入，位于「单数据源·双派生视角」管理线一端（与指标/压降同族）；③ 界面层——基层屏只读（带"上级定义"标签），真正定义维护在上方的管理配置模块。
> 方法数量以管理层/客户实际定义为准（演示标注"待上面定义确认"）。

---

## 五、预览（8123 运行中）

- 编制视图（费用责任岗，看 ③ 只读方法 + 控制基线）：`http://localhost:8123/?as=expense#compile`
- 基层首屏（运营监控中枢，看 ② 重构后工作台）：`http://localhost:8123/?as=staff`

---

## 六、下一步

1. 你确认 §四 后，我把「定义与定位」落进 V1 + 映射文档，复跑校验再交付。
2. 编制期的"窗口期开放填报 / 汇总"交互（当前为倒计时/只读占位）如需细化，可排入下个迭代。
3. 后端 agent（M7 风险筛查 / 压降引擎服务端化）仍按 08-22 09:00 汇报结论：**待你拍板是否启动**。
